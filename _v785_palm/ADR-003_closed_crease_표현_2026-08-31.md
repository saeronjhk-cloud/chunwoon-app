# ADR-003 — closed crease(RLC·PTC 공통 주름)를 어떻게 표현할 것인가 (P-61)

> **상태**: ★★**승인** (제이 · 2026-08-31) — ★**§4-1 5개 항목 구현 완료** (P-64)
> ★구현 결과와 회귀는 `천운_손금_인수인계_v7.2_2026-08-31.md` §3 을 보십시오.
> **정본 위치**: `D:\AI ChunWoon\_v785_palm\ADR-003_closed_crease_표현_2026-08-31.md`
> **날짜**: 2026-08-31 · **승계**: ADR-001(승인) · ADR-002(승인) · 인수인계 v7
> **관련 과제**: ★P-61 · P-62 · P-33(G-7) · 라벨규칙 v1 §4-2

---

## 0. 한 장 요약

| | |
|---|---|
| **문제** | 조각 하나에 클래스 하나만 주는 현재 구조가 ★Closed(한국인 최다수 패턴)를 표현하지 못합니다 |
| ★**새로 밝혀진 것** | ★그 조각이 ★막는 것은 라벨이 아니라 ★**T2 판정축**입니다 — ★단 ★「항상」이 아니라 ★**U-Net 이 공통 구간을 한 조각으로 낼 때**입니다 (23장 중 1장 관찰 · §2-3 · §2-6) |
| **결정** | ★**안 B(별도 클래스 `CLOSED`) 어휘만 채택** + ★**안 C(meta 기록) 유지** · ★**안 A(다중 라벨) 기각** |
| ★**지금 학습하지 않는 이유** | ★양성 표본이 ★**1건**입니다. n=1 은 ★평가가 불가능하고(고정 원칙 4 Eval-First), ★212 집합을 건드려 ★72.2 %를 흔듭니다 |
| **승격 조건** | ★Tier-B 에서 `CLOSED` ★**10건 이상**(★이 ADR 의 제안치 · §4-2) → 그때 평가셋으로 구현안 결정 |
| ★**미결로 남김** | ★**P-67** — `CLOSED` 검출 시 T2 가 무엇을 답하는가 · ★**P-68** — `note` 정본 채널 정리 |
| ★**212 · 72.2 % · 0.766** | ★**불변**입니다. 이 결정은 학습 집합을 건드리지 않습니다 |

---

## 1. 계기 — 제이가 표시하신 1건

2026-08-31 조각 라벨에서 제이가 `note` 에 **`RLC & PTC`** 로 적으신 조각 1건:

```
KakaoTalk_20260824_132401322_13.jpg  frag 0
npix 328   cu -0.014   cv 0.620   u_max 0.316   v_max 0.925   ang_v_deg 22.2
```

이것이 **Park 2010 의 Closed** 이고, 라벨규칙 v1 §4-2 가 「한국인 **최다수** 패턴」이라 적어 둔 바로 그것입니다.

---

## 2. ★★실측 — 무엇이 실제로 막히는가 (`p61_closed.py` · 2026-08-31)

> ★★**2026-08-31 적대적 감사 반영.** 초안의 과장 6건을 아래에 ★그대로 교정해 두었습니다.
> ★특히 「**Closed 이면 조각이 하나로 나온다**」는 ★n=1 일반화였습니다 — §2-6 이 그 반증입니다.
> ★`p61_closed.py` 는 ★dev 진단이며 ★신설 임계 2개(`nseg` · `minpix`)를 쓰므로 ★verdict 근거가 아닙니다.

### 2-1. 그 조각은 데이터셋 **최대 조각**입니다

★★**「단일 연결성분」은 측정 결과가 아닙니다.** `palmtype.components()` 가 8-연결 성분을 내므로
 ★조각 하나가 연결성분 하나인 것은 ★정의상 참입니다. ★초안의 「★단일 연결성분 (실측)」 표기는 ★철회합니다.

| | 값 |
|---|---|
| npix | ★**328** — 218조각 중 ★1위 (백분위 99.5 %) |
| 전 조각 npix 중앙값 | 12 (★27배) |
| 2~8위 | ★**전부 `RLC`** (235 · 221 · 205 · 198 · 195 · 187 · 176) |

⟹ ★「크다」만으로는 병합 조각을 가릴 수 없습니다. **긴 RLC 호(arc)와 크기가 겹칩니다.**

### 2-2. ★★그 조각은 v3 규칙의 **RLC 영역과 PTC 영역에 걸쳐 있습니다**

주축 방향 2등분 후 각 구간에 **v3 규칙을 그대로** 적용:

★★**「새 임계 없음」은 틀렸습니다 (감사 지적 · 교정)**. 아래 결과는 임계를 ★3중으로 씁니다:
```
① nseg = 2        ★신설 임계 (레지스트리 미등록)
② minpix = 8      ★신설 임계 — palmtype.py:46-48 이 「여기에 숫자를 적지 마십시오」라 못박은 그 종류
③ V3_UMAX_* 4개   ★DEV_DEFAULTS = ★Tier-C(스튜디오 박스 라벨) 유래
⟹ ★이 절의 수치는 ★NOT_A_VERDICT 입니다. ★G-7 을 통과하지 않았습니다.
```

```
전체 조각    → PTC
구간 1       → PTC        구간 2 → ★RLC
⟹ ★straddle(걸침) = 예
```

**오탐(특이도)**: 단일 클래스 정답 조각 중 검사 대상 **124건에서 오탐 5건 = 4.0 %** (DTC 4 · PTC 1).
★2-1의 큰 RLC 호 7개는 **하나도 걸리지 않았습니다.**
★★양성이 1건뿐이므로 **민감도는 잴 수 없습니다** — 고정 원칙 4에 따라 **지금 승격하지 않습니다.**

### 2-3. ★★★진짜 피해는 라벨이 아니라 **T2 판정축**입니다 (이번 세션 최대 발견)

`palmtype.judge_T2` 는 **Closed / Meeting / Open 을 가르는 축**입니다 — 즉 **closed crease 를 답하라고 만든 함수**입니다. 그런데:

```python
palmtype.py:366   if not groups["RLC"]: return _uncertain("T2-U", "RLC_NOT_DETECTED")
palmtype.py:367   if not groups["PTC"]: return _uncertain("T2-U", "PTC_NOT_DETECTED")
palmtype.py:368   gap = _pairdist(feats, groups["RLC"], groups["PTC"])
```

★T2 는 **RLC 조각과 PTC 조각이 따로 존재해야만** 잴 수 있습니다.
★그런데 **U-Net 이 공통 구간을 한 조각으로 낼 때는** 조각이 하나로 나옵니다.

⟹ ★**그 경우 T2 는 Closed 를 판정하지 못합니다.**

### ★★초안의 과장을 교정합니다 (감사 지적)
```
초안: 「Closed 일수록 Closed 를 판정하지 못한다 — 자기봉쇄」
★철회합니다. ★n=1 일반화였습니다.
★정확한 범위: 「U-Net 이 공통 구간을 ★한 조각으로 낼 때, ★그 장에서 T2 가 막힌다」
★관찰 빈도: ★23장 중 1장 (218조각 중 1조각)
★반증: §2-6 — ★20장에서는 RLC·PTC 가 ★따로 나오고 T2 는 ★정상 작동합니다
```

**세 갈래 귀결**

| 경우 | 결과 | 성질 |
|---|---|---|
| 병합 조각이 PTC 로 배정 · RLC 군 비어 있음 | `T2-U / RLC_NOT_DETECTED` | 판독 불가 (안전) |
| 병합 조각이 RLC 로 배정 · PTC 군 비어 있음 | `T2-U / PTC_NOT_DETECTED` | 판독 불가 (안전) |
| ★병합 조각이 한쪽 · **무관한 잡음 조각**이 다른 쪽 | ★**`T2-O`(Open)** | ★**거짓 판정 가능** — ★단 ★현재는 발화 불가 (아래) |

★★**세 번째는 ★현행 결함이 아니라 ★잠재 결함입니다 (감사 지적 · 교정)**
```
`T2_CLOSED` 는 ★FAILED_2026_08_26 에도 ★DEV_DEFAULTS 에도 ★없습니다.
⟹ verdict · diagnostic · dev ★세 모드 전부 ★THRESHOLD_UNSET → T2-U 로 끝납니다 (실측 확인).
⟹ ★★거짓 T2-O 는 ★freeze() 로 T2_CLOSED 가 확정된 ★이후에만 발화합니다.
⟹ ★그래서 ★지금 막아 두는 것입니다 — ★그때는 이 논의가 잊혀 있을 것이기 때문입니다.
```

### 2-4. ★연쇄 — T3 의 「없음」도 같이 죽습니다

```python
palmtype.py:432   ("T2_UNCERTAIN", t2["code"] != "T2-U"),
```
`T3-A`(운명선 **없음**)는 ★게이트 **3개**(T1 · T2 · 면적) 전부를 요구하며 T2 는 그중 하나입니다.
⟹ ★closed crease 로 T2 가 막히면 **FATE 「없음」 판정까지 봉인**됩니다.

★★**단, 그 1건에서는 이 인과가 ★비구속적입니다 (감사 지적)**
```
_13.jpg 정답 조각 = [CLOSED, DTC, FATE] ⟹ PTC 가 없습니다
⟹ judge_T1(palmtype.py:343-344)이 ★MISSING_PTC_OR_DTC 로 ★먼저 T1-U 를 냅니다
⟹ ★그 장에서는 T2 가 아니라 ★T1 이 T3-A 를 막습니다.
★T2→T3 봉인은 ★구조상 참이지만 ★이 사례가 그 증거는 아닙니다.
```

### 2-5. ★T2 판정 가능 장수 — 배정이 완벽해도 20/23

★**정답 조각 기준**(배정 규칙이 100 % 맞다고 가정)으로 RLC·PTC 가 둘 다 있는 장:

```
20 / 23 장 (87 %)
X _02.jpg  RLC=1 PTC=0   ← ★U-Net 이 PTC 를 못 낸 것 (커버리지 문제)
X _06.jpg  RLC=1 PTC=0   ← ★동上
X _13.jpg  RLC=0 PTC=0   ← ★CLOSED 를 우리가 제외한 결과 (★자기 결정의 산물)
```

★★**「구조적 상한」이라는 이름은 철회합니다 (감사 지적)**
```
★3장의 원인이 ★서로 다릅니다 — 둘은 ★U-Net 커버리지, 하나는 ★우리 제외 규칙입니다.
⟹ ★「판정축의 상한」이 아니라 ★「이 표본에서 T2 가 도달 가능한 장수」입니다.
⟹ ★1명·23장입니다. ★v7 §2(5-fold CV)와 ★같은 등급으로 인용하지 마십시오.
```

### 2-6. ★★반증 — Closed 가 항상 한 조각으로 나오는 것은 아닙니다

```
23장 중 ★20장에서 RLC·PTC 조각이 ★따로 존재합니다.
장당 조각 9.5개 · npix 중앙값 12 (v7 §2-6) ⟹ ★U-Net p>0.5 출력은 ★쪼개지는 것이 정상입니다.
⟹ 공통 구간이 분기점에서 끊기면 ★두 조각이 RLC/PTC 로 갈라지고 ★gap 이 작게 나와
   ★T2 는 오히려 ★정상 작동합니다 (그것이 T2-C 의 원래 설계입니다).
⟹ ★★따라서 병합은 ★「Closed 의 필연」이 아니라 ★「U-Net 출력의 우연」입니다.
⟹ ★그 빈도는 ★Tier-B 로만 알 수 있습니다 (P-65).
```

---

## 3. 세 안 비교

### ★안 A — 다중 라벨 (조각 하나에 `{RLC, PTC}`) → ★★기각

| 축 | 판정 |
|---|---|
| ★**순환논증** | `_pairdist(groups["RLC"], groups["PTC"])` 에 ★**같은 조각이 양쪽에** 들어갑니다 ⟹ `gap = 0` ⟹ ★**`T2-O`(Open)를 구조적으로 낼 수 없습니다**. ★판정이 라벨의 되풀이가 됩니다 <br>★★초안의 「항상 `T2-C`」는 ★틀렸습니다 (감사 실측): gap=0 이어도 `common_len` 에 따라 ★`T2-C` · `T2-M` · `T2-U`(NEAR_THRESHOLD) 로 갈립니다. ★막히는 것은 ★`T2-O` 하나입니다 |
| 코드 | `tree.py` 는 단일 라벨 Gini 트리입니다 (`CLASSES` · `_class_counts` · `_gini`). 다중 라벨은 ★재작성입니다 |
| 평가 | `bench_assign.score/report` 가 단일 라벨 전제 ⟹ ★**212 · 72.2 % · 0.766 이 무효화**됩니다 |
| 마스크 | 라벨규칙 v1 §4-2 「★중복 금지 — 마스크는 픽셀당 클래스가 하나」와 정면 충돌 |

⟹ ★**얻는 것보다 부수는 것이 큽니다. 기각합니다.**

### ★안 B — 별도 클래스 `CLOSED` → ★**어휘만 채택**

| 축 | 판정 |
|---|---|
| 단일 라벨 유지 | ✅ 트리·Gini·CV·벤치마크 ★형태 그대로 |
| ★순환논증 | ⚠ ★**「없음」은 과장이었습니다 (감사 지적)**. 안 B 의 가드도 ★라벨이 판정을 직결로 정합니다. <br>★차이는 ★「순환의 유무」가 아니라 ★**「확답이냐 판독불가냐」**입니다 — 안 A 는 ★확답을, 안 B 는 ★fail-closed 를 냅니다 |
| ★**지금 학습** | ❌ ★**양성 1건**. n=1 로 6번째 클래스를 학습시키면 ★평가가 불가능하고 ★212 집합을 건드려 ★72.2 %를 흔듭니다 |
| 어휘만 도입 | ✅ ★`groups["CLOSED"]` 키 신설 + T2 fail-closed 가드. ★학습 집합 불변 ⟹ ★수치 불변 |
| ★**미결로 남는 것** | ★**`CLOSED` 가 검출되면 T2 는 무엇을 답해야 하는가** — ★P-67 (§4-2) |

### ★안 C — meta 열 (`closed_crease: Y/N`) → ★유지

이미 라벨규칙 v1 §4-2 가 **이미지 단위**로 지시하고 있습니다. 폐기할 이유가 없습니다.
단 ★**이것만으로는 §2-3 의 거짓 `T2-O` 를 막지 못합니다** — meta 는 지표 산출에서 **제외**시킬 뿐, 엔진의 판정 경로는 그대로입니다.

---

## 4. ★★결정

```
① 안 A(다중 라벨)          → ★기각. 순환논증 + 212 무효화
② 안 B(별도 클래스 CLOSED) → ★★어휘·판정 경로만 채택. ★학습은 Tier-B 후
③ 안 C(meta 기록)          → ★유지 (라벨규칙 v1 §4-2 그대로)
```

### 4-1. 지금 하는 것 (★수치를 건드리지 않는 것만)

| # | 내용 | 영향 |
|---|---|---|
| ★1 | `frag_gt` 표기를 `RLC & PTC` → ★정식 값 **`CLOSED`** 로 통일 | ★학습·CV 집합에서 ★계속 제외 ⟹ ★**212 불변** |
| ★2 | `palmtype` 에 `CLOSED` 를 ★배정 어휘로 등록 · `groups["CLOSED"]` 키 신설 | 예측기가 아직 못 냅니다 (n=1) ⟹ ★빈 리스트 |
| ★★3 | `judge_T2` 맨 앞에 ★fail-closed 가드: `groups["CLOSED"]` 가 비어 있지 않으면 ★`T2-U / reason="CLOSED_FRAGMENT_UNMEASURED"` | ★§2-3 세 번째 줄(거짓 `T2-O`)을 봉쇄. ★**임계 불필요** <br>★★이것은 ★**잠정값이지 최종 의미가 아닙니다** — ★P-67 |
| 4 | `meta.csv` `closed_crease` 를 ★조각 단위로도 기록 | 지표 산출 시 분리 |
| ★5 | ★라벨규칙 v1 §4-2 개정 — §5 문구 | GT 정의 명확화 |

★**3번은 지금은 발화하지 않습니다** — 예측기가 `CLOSED` 를 낼 수 없기 때문입니다.
★**Tier-B 로 학습이 붙는 순간 자동으로 무장됩니다.** 미리 넣어 두는 이유는, 그때 잊지 않기 위해서입니다.

### ★★4-1-b. 가드의 한계 — ★두 가지를 정직하게 적습니다 (감사 지적)

```
① ★가드는 「구조적 봉쇄」가 아닙니다.
   ★`groups["CLOSED"]` 가 채워질 때만 발화하므로 ★실효성은 ★CLOSED 분류기의 ★재현율에 달렸습니다.
   ★미탐(병합 조각을 그냥 PTC 로 배정)이면 ★가드는 침묵하고 ★거짓 T2-O 경로가 그대로 열립니다.
   ⟹ ★가드는 ★안전망이지 ★해결이 아닙니다.

② ★★가드는 ★알고 있는 답을 버립니다.
   ★라벨규칙상 `CLOSED` 조각 = ★Park 의 Closed 입니다.
   ⟹ ★그 라벨이 붙는 순간 T2 의 답은 ★이미 `T2-C` 인데, ★가드는 `T2-U` 를 냅니다.
   ⟹ ★이대로 두면 ★한국인 최다수 패턴이 ★영구 판독 불가로 고정됩니다.
   ⟹ ★★그래서 ★P-67 을 신설합니다 — ★가드는 ★해제 계획이 붙은 잠정 조치입니다.
```

### 4-2. Tier-B 후에 하는 것 (★지금 정하지 않습니다)

★★**신설 P-67 — `CLOSED` 가 검출되면 T2 는 무엇을 답하는가**
```
후보 ⓐ T2-C 직결        — ★가장 단순. ★그러나 라벨이 곧 판정 = ★순환. ★confidence 를 못 줍니다
후보 ⓑ 조각 ★내부 측정  — ★조각을 공통 몸통 / 두 갈래로 나눠 ★common_len 을 ★조각 안에서 잽니다
                          ⟹ ★judge_T2 의 원래 정의(common_len ≥ T2_COMMON_LEN)를 ★그대로 씁니다
                          ⟹ ★순환이 아닙니다. ★단 분기점 검출이 필요합니다
후보 ⓒ T2-U 유지         — ★현재 잠정값. ★최다수 패턴을 영구 판독불가로 두는 대가가 큽니다
★★Tier-B 후 ★평가셋으로 결정합니다. ★「느낌으로 더 좋다」 금지.
```

★승격 조건: **Tier-B 에서 `CLOSED` 정답 ≥ 10건**

★★**근거 표기 정정 (감사 지적)**
```
★제이의 ★고정 원칙 4(Eval-First)는 ★「IP v1→v2 전에 평가셋 10~20 케이스」입니다 — ★프로젝트 외부의 상시 원칙입니다.
★이 트랙 문서가 적어 둔 평가셋 규모는 ★40~50장입니다 (ADR-001:214 · 유형체계 v1:69) — ★단위가 다릅니다(장 vs 케이스).
⟹ ★「10건」은 ★고정 원칙 4의 하한을 ★조각 단위로 옮긴 ★이 ADR 의 제안치입니다.
⟹ ★저장소 문서에서 따온 값이 ★아닙니다. ★제이가 결재 시 조정하실 수 있습니다.
```

그때 아래 둘을 **평가셋으로** 겨룹니다. ★「느낌으로 더 좋다」 금지.

```
구현안 ㉮ 6클래스 트리   — CLOSED 를 클래스로 직접 학습
구현안 ㉯ straddle 검사  — 조각을 주축 2등분해 v3 를 각각 적용, 클래스가 갈리면 CLOSED
                          ★현재 오탐 4.0 % (5/124) · ★민감도 미측정
```
★㉯를 verdict 로 올리려면 ★신설 임계 **2개**가 필요합니다 (`STRADDLE_NSEG` · `STRADDLE_MINPIX`) ⟹ ★G-7 · P-33 대상.

### 4-3. ★하지 않기로 한 것

```
✗ 지금 6번째 클래스를 학습에 넣기          — n=1 · 원칙 4 위반 · 72.2 % 오염
✗ 다중 라벨                                 — 순환논증 (gap=0 ⟹ 항상 T2-C)
✗ straddle 을 지금 verdict 규칙으로 승격    — 민감도 미측정 · 신설 임계 2개
✗ 「크면 병합 조각」 규칙                    — 2~8위가 전부 단일 RLC 입니다
✗ 라벨규칙 v1 §4-2 의 「공통 구간 → RLC」 폐기 — ★픽셀 마스크 층은 그대로 둡니다 (§5)
```

---

## 5. ★라벨규칙 v1 §4-2 개정 문구 (승인 시 반영)

★**층을 나눕니다.** 지금까지 §4-2 는 두 층을 한 규칙으로 다뤄 혼선이 있었습니다.

| 층 | 대상 | 규칙 | 변경 |
|---|---|---|---|
| **L1 픽셀 마스크** | 사람이 칠하는 GT | 공통 구간 → `1`(RLC) · 애매하면 `255` | ★**변경 없음** |
| ★**L2 조각 배정** | U-Net 출력 조각의 정답 | ★공통 주름이 ★한 조각으로 나오면 → ★**`CLOSED`** | ★**신설** |
| **L3 판정(T2)** | 엔진 출력 | Closed / Meeting / Open / Uncertain | ★가드 신설 (4-1 ③) |

★§4-2 가 스스로 적어 둔 「**이것은 GT 를 체계적으로 왜곡합니다**」는 ★**L1 에 한해 유효**합니다.
★L2 에서는 왜곡되지 않습니다 — `CLOSED` 라는 자기 값을 갖기 때문입니다.

⟹ ★§4-2 말미의 「필수 대응」은 다음으로 대체합니다:
```
· L1: meta.csv 에 closed_crease: Y/N 기록 · 지표 산출 시 PTC 근위부를 별도로 본다  (기존 유지)
· L2: 조각 정답에 CLOSED 를 쓴다 · ★Tier-B 전까지 학습·CV 집합에서 제외한다      (신설)
· L3: CLOSED 조각이 있으면 T2 는 fail-closed 로 T2-U 를 낸다                      (신설)
```

---

## 6. 영향 범위 — ★재작업이 필요한 것 / 아닌 것

### ★불변 (다시 재지 마십시오)

```
✅ 조각 정답 212개 · GT 분포 NONE 82 · PTC 45 · RLC 37 · DTC 29 · FATE 19
✅ ① 45.8 % · ★② 72.2 % / F1 0.766 · ③ 58.5 %      (학습 집합을 건드리지 않습니다)
✅ 박스 GT: v1 44.9 · v2 41.5 · v3 69.9 · tree 76.6 %
✅ 병합 P-24 · 임계 P-50 · 도메인 P-58 · 캐시 npz 5개 · 특징 13개 순서
```

### ★손대는 파일

| 파일 | 변경 | 회귀 |
|---|---|---|
| `palmtype.py` | `CLOSED` 어휘 · `groups` 키 · `judge_T2` 가드 | ★**갱신 불필요** — 실측 확인: `t_palmtype.py:115` 는 ★키 개수가 아니라 `sum(groups.values()) == n_frags` 를 봅니다. ★빈 `CLOSED` 키를 더해도 합은 그대로입니다. ★단 가드가 발화하는 케이스는 ★신규 검사 1건을 추가하십시오 |
| ★`ourtree.py` | ★**변경 없음** — `OK = set(tree.CLASSES)`(ourtree.py:31)이므로 `CLOSED` 는 자동으로 제외됩니다 | 변경 없음 |
| `bench_assign.py` | ★**변경 없음**. ★초안이 적은 「`OK` 집합」은 ★이 파일에 ★없습니다 (박스 GT `NAME2CLS` 만 있습니다) — ★표기 오류 정정 | 변경 없음 |
| `frag_gt(수정).csv` | `RLC & PTC` → `CLOSED` (1행) | ★제외 대상은 그대로 6건 |
| ★**`p61_closed.py`** | ★①의 하드코딩 `"RLC & PTC"` 를 ★`CLOSED_TAGS` 로 교체 ★(2026-08-31 반영 완료) | ★안 고쳤으면 ①이 ★조용히 사라집니다 |
| ★**`seeassign.py`** | ★`COLOR[owner.get(i,'NONE')]`(28-29,45) — ★5색 dict 직접 인덱싱 ⟹ ★`CLOSED` 예측 시 **KeyError** | ★색 1개 추가 필요 |
| ★**`fraglabel.py`** | ★동上 (80행) | ★동上 |
| 라벨규칙 v1 §4-2 | §5 문구 | — |

### ★★놓치기 쉬운 결합 3건 (감사 지적 · 반드시 읽으십시오)

```
① ★★tree.CLASSES ↔ ourtree.OK 결합
   ourtree.py:31  OK = set(CLASSES)   ← ★tree.CLASSES 를 그대로 씁니다
   ⟹ ★구현안 ㉮(6클래스 트리)를 채택해 ★CLASSES 에 CLOSED 를 넣는 ★순간
      ★ourtree.OK 가 ★자동으로 넓어져 ★212 → 213 이 되고
      ★★72.2 % · F1 0.766 · 45.8 % · 58.5 % ★전부가 재계산 대상이 됩니다.
   ⟹ ★㉮ 채택 시에는 ★반드시 ★OK 를 명시 집합으로 분리하고 ★재측정을 ADR 에 남기십시오.

② ★G-6(판정 어휘) 주의
   harness.py:29  BANNED = (..., "FAIL", "pass", "fail")  · G-6 은 rows/payload ★전문을 봅니다.
   ⟹ ★reason="CLOSED_FRAGMENT_UNMEASURED" 는 ★안전합니다.
   ⟹ ★그러나 res["notes"] 에 ★「fail-closed 가드 발화」 같은 ★한글 문구를 넣으면
      ★Tier-A 리포트가 ★GateFail 로 죽습니다. ★"fail" 문자열을 쓰지 마십시오.

③ ★t_palmtype.py:115 는 ★키 개수가 아니라 sum(groups.values()) == n_frags 를 봅니다
   ⟹ ★빈 CLOSED 키 추가는 ★안전합니다 (실측 확인 · 239/239 유지).
   ⟹ ★단 가드가 발화하는 케이스는 ★신규 검사 1건을 추가하십시오.
```

★**게이트**: G-1~G-8 ★영향 없음 (★②의 조건부 주의 포함). 신설 임계가 없으므로 ★G-7 도 영향 없습니다.
★**freeze()**: ★여전히 20~25명 전에 부르지 마십시오. 이 결정은 그것을 앞당기지 않습니다.

---

## 7. ★부수 정정 (v7 §3 수치)

v7 §3 은 「제이가 고친 것 **100/218 (46 %)**」이라 적었습니다. ★실측 재계산:

```
note 열 기입          105행 (클래스값 99 · 비고 6)
★유효 212건 중 pred_tree 와 다른 것 = ★94건 (44.3 %)
⟹ ★tree 의 우리 도메인 정확도 55.7 % 와 정확히 맞습니다 (100 − 44.3)
```
⟹ ★앞으로는 **94/212 (44.3 %)** 를 인용하십시오. 「100/218」은 폐기합니다.
★`gt` 열은 218행 전부 `pred_tree` 와 동일 — 제이는 `note` 에만 적으셨고, `ourtree.read_gt` 의 note-우선 규약은 ★맞습니다 (실측 확인).

### ★★그러나 `note` 를 정본 GT 채널로 두는 것은 위험합니다 (감사 지적 · 신설 과제)

```
① ★라벨규칙 v1 §5 는 자유 텍스트 `note` 열을 ★「감사 지적 10 — 금지 정보가 흘러들 통로」라며
   ★삭제했습니다. ★그런데 frag_gt 에서는 그 열이 ★정답 채널이 돼 있습니다.
② ★ourtree.read_gt(ourtree.py:38)는 ★오타 note 를 ★검증 없이 「클래스 아님」으로 처리해
   ★표본에서 조용히 탈락시킵니다 (경고는 skipped 출력뿐).
   ⟹ ★note 오타 하나로 ★212 가 소리 없이 줄어듭니다.
③ ★gt 열은 218행 전부 pred_tree 와 같아 ★사실상 비어 있습니다.

⟹ ★★P-68 (신설): ★정정을 gt 열로 옮기고 note 를 비고로 되돌린다.
   ★단 ★옮기기 전후로 ★212 · 분포 · 72.2 % 가 ★동일한지 확인하고 옮깁니다.
   ★이 절에는 「재조사 금지」를 ★붙이지 않습니다 — ★정리해야 할 부채입니다.
```

---

## 8. 제이 결재란

```
■ ★승인 (2026-08-31) — 4-1 의 5개 항목을 ★전부 구현한다
□ 부분 승인 — (               ) 만 구현한다
□ 반려 — 이유:
```

### ★구현 결과 (2026-08-31 · P-64)

| # | 항목 | 상태 |
|---|---|---|
| 1 | `frag_gt_v3.csv` 에 `CLOSED` · 학습 제외 유지 | ✅ ★212 불변 검증 |
| 2 | `palmtype.CLOSED` · `GROUP_KEYS` · 3규칙 전부 키 생성 | ✅ |
| 3 | `judge_T2` fail-closed 가드 (`CLOSED_FRAGMENT_UNMEASURED`) | ✅ ★임계 불필요 |
| 4 | `seeassign`·`fraglabel` 색 추가 + `meta` 조각 단위 열 | ✅ |
| 5 | 라벨규칙 v1 ★§4-2-b — L1/L2/L3 3층 분리 | ✅ |

★**회귀**: 239 → ★**255/255** (t_palmtype 74 · t_gates 62 · t_mask 71 · t_betti 48)
★**불변 확인**: 212 · 분포 · ① 45.8 % · ② 72.2 % / 0.766 · ③ 58.5 % · 박스 GT 44.9/41.5/69.9/76.6 % ★전부 동일
★**신규 미결**: ★P-67(검출 시 T2 응답) · P-65(발생 빈도) · P-66(민감도 → ㉮/㉯ 결선)

★승인하시면 다음 세션이 §4-1 ①~⑤를 구현하고 `t_palmtype.py` 회귀를 갱신합니다.
★반려하셔도 ★§2 의 실측(특히 **2-3 T2 자기봉쇄** · **2-5 상한 87 %**)은 그대로 유효합니다.

# [2026-09-21] 천운 — ★v788 인수인계 · 꿈 엔진 가동(P-786-B) + 데일리 variance 전용 키(P-786-C) + IP 이관(P-786-H) ★정본
> **정본 위치**: `D:\AI ChunWoon\천운_인수인계_v788_꿈엔진_variance키_IP이관_2026-09-21.md`
> **승계 (폐기 금지)**: v787 최종 정본(`천운_인수인계_v787_최종_센서결속_게이트복구_2026-09-16.md` · §1~§10 전부 유효 — 특히 §4 분할 실행 표·§7 하지 말 것) · v787 중간본 · v786 정본 · v7.68 정본 · 손금 v7.2 정본 · ADR-001~003 · **ADR-FACE-001(⬜ 결재 대기 · 제이 회신 없음)**
> **커밋 (로컬 main · origin 대비 ★20커밋 미배포 — 이 문서 `479e8ce` + 참조 정정 커밋 포함)**:
> (참조 정정) ← `479e8ce` 인수인계 ← `39be05c` **P-786-H IP 이관** ← `f37d5a6` p12 평가셋 ← `f38822f` **P-786-C variance 키** ← `364b509` **P-786-B 꿈 엔진** ← `ad21a41`(v787 최종)
> **노션**: ★★v788 요약 https://app.notion.com/p/3e2e844981478164ad79f3332e6b0f9e (부모: 📑 천운 개발 일지 — 종합 인덱스) · v787 최종 https://app.notion.com/p/3dde84498147819b99a0f4f5d58c51b3 · ★노션은 요약, **이 md 가 정본**.
> **백업**: `backup_pre_v788/{_gate_pkg,IP_face}` (이관 전 리포 사본 · untracked · sha 대조 완료 · 지워도 됨)
> **제이 결정 (이 세션)**: ① 착수 4트랙 전부 승인(첫 10분 점검 · ADR 지원 · P-786-H · P-786-B/C) ② `D:\ChunWoon_IP` 폴더 생성·연결 ③ ★ADR-FACE-001 **회신 없음**(결재안 제시 상태) ④ ★push **미승인**(미실행)

---
## 0. ★★★한 장 요약 — 이 세션이 바꾼 것
| # | 트랙 | 한 것 | 실측 |
|---|---|---|---|
| ★1 | **P-786-B** | 꿈 `DREAM_KEYS` 12항목(v7.5x 이래 **참조 0건**)을 결정론 엔진 `js/dream_engine.js` 로 가동 — 줄거리→모티브 매칭→사전 풀이·길흉·행운수. LLM 결과 **위에 얹히고**, LLM 실패 시 **주 풀이**가 된다 | p11 **29/29**(오탐 함정 6종 차단) · jsdom 실구동 p11b **8/8** · 서버·ctx 키 **불변** |
| ★2 | **P-786-C** | 데일리 한 마디 결정변수(일진·시진·성향 버킷·회차)를 `personaTone` 접미 반송(v7.86 ③) → **전용 ctx 키 `variance`** 로 분리. 서버가 읽어 system 프롬프트 **별도 줄**로 보간, 없으면 줄 자체 생략 | p12 **11/11**(프롬프트 바이트 판정 · 개행 주입·400자 팽창 차단) · pin 수동 갱신 2건 |
| ★★3 | **P-786-H** | `_gate_pkg/`(550파일 42MB) · `_v786_diag/IP_face/`(12파일) → **`D:\ChunWoon_IP\{gate_pkg,face}`** 이관. 리포 스크립트는 env 없이 **형제 폴더를 자동 해석** | sha256 **550/550 · 12/12** 일치 · 관상 게이트 전종 재통과 · `D:\ChunWoon_IP\README.md` 작성 |
| 4 | ADR-FACE-001 | 결재안(A안 + R3·R5 값) 제시 · **R2 법무 자문 패킷 초안** 2건 작성(untracked · `천운_*.md` 무시 규칙) | 제이 회신 대기 |
| 5 | 공통 | **전 게이트 최종**: tools 3/3 · eval **57/57** · mut **13/13** · p04 191 · p05 46 · p06 77 · p07 8 · p08 36 · p09 20 · sync drift=0 | 이관 **후** 재측정치 |

★**사용자에게 바뀌어 보이는 것**: ① 꿈해몽 결과에 「📖 전통 해몽 사전 풀이」 블록(모티브 히트 시에만 · LLM 실패 시에도 나옴). ② 데일리 한 마디는 겉보기 동일(프롬프트 구조만 정리). ③ 관상은 변화 없음(ADR 미결재 · VERDICT 여전히 비어 있음).

---
## 1. ★★다음 세션 첫 10분 — 이어받기 확인
★**폴더 2개를 함께 추가**: `D:\AI ChunWoon` **+ `D:\ChunWoon_IP`**. IP 폴더가 없으면 run_gate 가 「패키지 없음 → 51종 침식 FAIL」, 관상 p0x 는 `[ip_face_root]` 예외 — **둘 다 의도된 fail-closed** 다. 재조사하지 말 것.
```bash
cd "D:\AI ChunWoon"
git log --oneline -3                  # (참조 정정) · 479e8ce(이 문서) · 39be05c
git status --short | grep -v backup_  # "결과 저장/" · package-lock.json · 자문 md 2건 만 untracked 여야 정상
node tools/sync_gate_pkg.js --check   # pkg=…/ChunWoon_IP/gate_pkg · drift=0
node -e "console.log(require('./_v786_diag/_ip_face_root.js')())"   # …/ChunWoon_IP/face
node _v788_work/p11_dream_engine_eval.js | tail -1      # 29/29
node _v788_work/p12_daily_variance_eval.js | tail -1    # 11/11
node _v786_diag/p08_face_verdict_eval.js | tail -1      # 36/36
node tools/run_gate.js tools                            # total=3 fail=0
node tools/run_gate.js eval:1/16                        # fail=0  (전건은 v787 §4-3 분할 표 · eval:k/16 은 k=2,14 만 ~170s)
```
★**샌드박스 함정 (v787 §1 두 건 + 신규 한 건)**
1. 첫 git 명령 전에 **삭제 권한**(`device_request_delete_permission` · 이 세션은 승인됨) — 아니면 `.git/index.lock` 스테일 락. 락이 있으면 `rm -f .git/index.lock`.
2. 도구 호출 **180초** 상한. `run_gate all` 금지. `eval:k/16` 은 k=2·14 가 ~170s(단독 호출), 나머지는 3개씩 한 호출 OK.
3. ★**신규**: `device_commit_files` 는 같은 `stagedPath` 를 두 번 보내면 **첫 업로드 캐시를 재사용**한다(내용을 고쳐도 옛 바이트가 써진다 — 이 세션 p11 E-2 수정 때 실측). ⟹ 파일 수정은 **`device_bash` 안에서 python read-modify-write** 로 하라. commit_files 는 신규 파일 1회 전달용.
4. 커밋 author 는 `--author="Claude (v788 session) <saeronjhk@gmail.com>"` 관행(리포 user.name 은 제이).

### 제이 회신에 따라 갈리는 첫 행동
| 제이가 준 것 | 첫 행동 |
|---|---|
| ★**ADR-FACE-001 결재**(A안) | `D:\ChunWoon_IP\face\doctrines.json` `FACE_XLHZ.user_output_enabled`… ★단 **R1(영인 바이트)·R2(법무) 미충족이면 켜지 않는다** — 결재는 「방향+R3·R5 확정」이고 실제 플래그는 R1·R2 채워진 세션에서 `node _v786_diag/compile_face_rules.js` → p08 I-6 |
| ★**push 승인** | `git push origin main`(20커밋) → Vercel 배포 → **P-787-C 브라우저 실기 검증**(v787 §6-3) + ★**꿈 엔진 실기**(§2-4) |
| 영인 파일(相理衡眞 10冊) | `D:\ChunWoon_IP\face\sources\` 에 넣고 sha256 → `source_editions.json` `SRC_XLHZ_A.scan_file_sha256` 등재 → Commons sha1 과 대조 (R1 해소) |
| 법무 자문 회신 | `법무/관상/회신_<패널>_<날짜>.md` 보관 → 채점표로 채점 → 충돌 대조표 → 변호사 의뢰 질문 5개 확정 |
| 얼굴 사진(동의) | P-787-A 센서 모집단 + P-786-A 랜드마크 모집단 동시(계측값만) |
| 없음 | P-786-E(LLM 출력 유사도 실측) · P-787-B(川字·懸針 정답률) · v7.71 패키지 zip · `package-lock.json` 처리 결정(§6) |

---
## 2. ★P-786-B 꿈 모티브 엔진 — 구조 (재조사 불요)
```
index.html  analyzeDream()
  engineHits = CW_DREAM_ENGINE.matchDreamKeys(story, DREAM_KEYS)   ← DREAM_KEYS 표는 index.html 그대로(사본 없음)
  info.engineHits 운반 → renderDreamResult(LLM 성공: 핵심상징 블록 뒤에 _cwDreamEngineBlock(hits,false))
                       → renderDreamFallback(LLM 실패: 오류 박스 앞에 _cwDreamEngineBlock(hits,true) = 주 풀이)
js/dream_engine.js  (UMD · window.CW_DREAM_ENGINE / module.exports)
  PATTERNS 12종 — 한 글자 키(용·물·불·산)는 「문두/공백/구두점 + 키 + 조사」 정규식 + mask(결합어 제거) 로만 잡는다
  matchDreamKeys(story, keys) → keys 원본 객체 배열(PATTERNS 순 · 중복 없음 · 표에 없는 키는 절대 생성 안 함)
  summarize(hits) → {fortuneType: 길몽|흉몽|평몽|null, numbers: ≤4(중복 제거), engine}
```
- **엔진 우선 원칙(플레이북 ⑤)**: 히트가 있으면 LLM 성패와 무관하게 화면에 실린다. 히트 0 이면 침묵(빈 배열) — 지어내지 않는다. 카테고리 선택만으로는 매칭하지 않는다(동물→12종 중 특정 불가).
- **서버로 보내지 않는다** — ctx 키를 늘리면 `eval_ctx_key_surface` K-2 · `eval_dream_daily_guard` C-2 가 붉어진다. 서버 프롬프트에 엔진 히트를 실으려면 §6 「P-788-A」 절차.
- **오탐 차단 실측**(p11 C 계열): 「사용·내용」≠용 · 「동물·선물·건물」≠물 · 「불안·불행」≠불 · 「계산·부산·산책」≠산 · 「팥죽·죽을 끓」≠죽음 · 「불꽃놀이」= 불(꽃 아님).
- **표를 고치면**: `PATTERNS` 수정 → `node _v788_work/p11_dream_engine_eval.js` 재실행(29/29) → `p11b`(8/8). 「느낌으로 더 좋다」 금지.
- 게이트 영향: index.html 은 sha pin 대상이 아니다. claim_whitelist(1)·client_port_drift(2)·ctx_key_surface(7)·response_scrub(49)·tmp_hygiene(53) 통과 확인.

### 2-4. ★P-788-C 꿈 엔진 브라우저 실기 (배포 후)
```
· 줄거리 「큰 돼지가 집으로 들어왔다」 → 결과 카드에 📖 전통 해몽 사전 풀이 · 🐷 돼지 · 길 · 숫자 4개
· 네트워크 차단(DevTools offline) 후 같은 입력 → ⚠️ 오류 박스 **위에** 사전 풀이(「AI 없이 사전 대조」 표기)
· 「회사에서 회의를 했다」 → 사전 블록 없음
· iOS Safari 15 이하는 미지원 브라우저(정규식 lookbehind 미사용이라 문법상 문제 없음 · UMD 래퍼만 확인)
```

---
## 3. ★P-786-C variance 전용 키 — 구조
```
js/chat.js  fetchDailyMessage()
  ctx = Object.assign({}, _gatherChatContext(), { personaName, personaTone: persona.tone, variance: _cwDailyVariance(), category })
  _cwDailyVariance() → '【오늘의 변주 씨앗】오늘 일진 ○○일 · 지금 ○시 · 성향 버킷 k/32 · 오늘 n번째 청. …(옮기지 말라 지시)'  (접두 ' · ' 제거됨)
api/fortune.js  daily_message 분기
  const variance = (typeof c.variance === 'string' && c.variance.trim()) ? '\n' + c.variance.trim() : '';
  systemPrompt = `당신은 "${personaName}"입니다. 캐릭터: ${personaTone}.${variance}\n사용자의 오늘 운세를…`
```
- ctxguard **1층 대상 아님**(사람 사실 키가 아니다) · **2층 평탄화는 받는다**(개행·제어문자 제거 · 400자 상한) — p12 V-5·V-6 실측.
- `eval_dream_daily_guard` `PROMPT_KEYS_KNOWN` 에 `'variance'` 등재(C-2 「무방비 자리 증가 차단」 표).
- **pin 수동 갱신** (regen 금지 유지): `eval/_gate_pins.json` sources `api/fortune.js` sha `c1af3dcb…→6846eb24…` · evals `eval_dream_daily_guard.js` sha `ef96a434…→db1a55c1…` · bytes_min/checks_min 불변 · `tools/run_gate.js` `GATE_PINS_SHA256 = a1362f2c70eb1a1f…` · sync 로 패키지 반영.
- 종전 인용 정정: v786 §6 「P-786-C 데일리 variance 전용 ctx 키 (지금은 personaTone 반송)」 → **해소**.

---
## 4. ★★P-786-H IP 이관 — 위치·해석·복구
```
D:\ChunWoon_IP\                (★git 밖 · Cowork 세션에 폴더 추가 필요)
  README.md                    구조·해석 순서·재조립
  gate_pkg\   550파일 42MB      = v787 §4-1 _gate_pkg 그대로 (v7.70 zip + eval_vercel_config + 동기화본 + node_modules + _internal_only 33MB)
  face\       12파일 364K       = 구 _v786_diag/IP_face (doctrines · rules/face/{ogwan,xlhz} · eval/*_gt · sources · promote_candidates_*)
해석 순서 (둘 다 동일 규약 · env > 형제 정본 > 구 위치):
  게이트 패키지: CHUNWOON_GATE_PKG > <ROOT>/../ChunWoon_IP/gate_pkg > <ROOT>/_gate_pkg          (tools/run_gate.js · sync_gate_pkg.js)
  관상 IP      : CHUNWOON_IP_FACE  > <ROOT>/../ChunWoon_IP/face     > _v786_diag/IP_face         (_v786_diag/_ip_face_root.js · doctrines.json 존재로 판정 · 없으면 throw)
사용처: compile_face_rules · p04 · p05 · p06 · p07 · p08 · p10  (7 스크립트가 해석기 한 곳을 require)
```
- Linux VM 마운트에서 `<ROOT>/..` = `$HOME/mnt/` 이므로 형제 폴더 규약이 Windows 네이티브(`D:\`)와 VM 양쪽에서 같은 코드로 동작한다 — 이것이 env 대신 형제 경로를 택한 이유.
- **재조립**(gate_pkg 소실 시): v787 §4-1 절차를 `D:\ChunWoon_IP\gate_pkg` 를 대상으로 (`unzip … -d D:\ChunWoon_IP\gate_pkg && cp … && node tools/sync_gate_pkg.js`).
- **face 소실 시**: `backup_pre_v788/IP_face`(리포 · untracked) 복사. 이 백업은 이관 시점 스냅샷이며 이후 p10/p05/p06 이 정본을 바꾸면 낡는다 — **정기 백업 대상은 `D:\ChunWoon_IP` 전체**.
- p05·p06 은 `promote_candidates_*.json` 을 정본에 **다시 쓴다**(generated_at 만 바뀜 · 내용 동일 실측). 게이트 결과에 영향 없음.
- `.gitignore`·`.vercelignore` 의 `_gate_pkg/`·`_v786_diag/IP_face/` 항목은 **그대로 둔다**(구 위치 재출현 방지).

---
## 5. ADR-FACE-001 — 결재안(제시됨 · 미회신) + R2 패킷
결재안(한 줄 「A안 제안대로」로 채택 가능): ① 방향 **A**(TEXT·FULL 5건 한정) ② R3 = 해당 5건 GT 100%(현재 충족 · 「원문 재현 정확도」이지 「관상이 맞는가」가 아님을 기준 문구에 명기) ③ R5 고지 = 「이 항목은 고전 문헌의 서술을 계측값에 적용한 결과이며, 얼굴이나 운세에 대한 사실 판단이 아닙니다.」(BAN_B04 회피) ④ R2 = 자문 패킷 초안 완료 → 검토 의뢰는 제이 ⑤ R1 = 영인 파일 직접 수령 필요.
★**A안 채택 ≠ 오늘 켜기**: R1·R2 가 비어 있는 한 `user_output_enabled` 는 false 유지.
R2 패킷(untracked · `천운_*.md` 무시 규칙 · 손금 트랙 관행과 동일하게 git 밖): `천운_관상법무자문_AI패널_질문서_2026-09-21.md`(Q1~Q10 · Part1 자료 미제시) · `천운_관상법무자문_채점표_INTERNAL_2026-09-21.md`(100점 · 패널 비공개).

---
## 6. ★★남은 것 (우선순위)
1. **ADR-FACE-001 결재**(제이) 2. **push 승인·배포**(19+커밋) 3. **P-787-C + P-788-C 브라우저 실기** 4. R1 영인 바이트 수령 · R2 자문 발송(제이) 5. P-787-A 센서 모집단(동의 선결) 6. P-787-B 川字·懸針 정답률 7. P-786-A/E/F/G1 · v7.71 패키지 zip(동기화본 6파일 + eval_vercel_config 포함)
- **P-788-A(선택)**: 꿈 엔진 히트를 서버 프롬프트에도 싣기 — 절차: ctx 키 `engineMotifs`(문자열) 신설 → `api/fortune.js` dream 3분기가 `c.engineMotifs` 읽음 → `eval_dream_daily_guard` PROMPT_KEYS_KNOWN 등재 → pin 2건 수동 갱신 → sync. **P-786-C 와 같은 절차**(§3). 지금은 클라 표시만으로 충분해 보류.
- `package-lock.json`(untracked · 2026-09-04 · 19KB): v787 정본 §1 은 「이제 없음」이라 적었으나 **실재**한다. 커밋할지 `.gitignore` 에 넣을지 제이 결정(Vercel 빌드에 영향 가능 — 현재 배포는 lock 없이 되고 있음).

---
## 7. ★★★하지 말 것 (v788 추가 · v787 §7 · v786 §7 전부 유효)
```
── 꿈 엔진 ──
✗ index.html 의 DREAM_KEYS 를 js/dream_engine.js 로 복사(사본) — 엔진은 표를 **인자로 받는다**
✗ PATTERNS 에 한 글자 키를 조사·마스크 없이 추가(「용」 → 「사용」 오탐) · 카테고리 선택만으로 히트 생성
✗ 엔진 히트를 ctx 에 싣기(K-2·C-2 붉어짐) — 필요하면 §6 P-788-A 절차
✗ 표를 고치고 p11/p11b 안 돌리기
── variance ──
✗ personaTone 에 다시 덧붙이기 · variance 를 ctxguard 교체 표(DAILY_CTX_KEYS)에 넣기(사람 사실이 아니다)
✗ PROMPT_KEYS_KNOWN 갱신 없이 서버 보간 키 추가 · pin 갱신 없이 fortune.js 수정
── IP 이관 ──
✗ D:\ChunWoon_IP 안의 무엇이든 리포로 복사·커밋 (원문 한자 · --no-verify 금지)
✗ _ip_face_root.js 의 throw 를 「없으면 빈 규칙」으로 완화 · 형제 경로 규약을 절대 경로(D:\)로 하드코딩
✗ backup_pre_v788 을 정본으로 착각(이관 시점 스냅샷)
── 인용 ──
✗ 「eval 57/57」을 D:\ChunWoon_IP 없이 재현 가능한 수치로 — 패키지 결속 상태의 결과
✗ 「꿈 엔진이 해몽을 한다」 — 엔진은 **12모티브 사전 대조**다. 나머지는 LLM
```

---
## 8. 자산 목록 (v788 추가·변경 전체)
| 경로 | git | 내용 |
|---|---|---|
| ★`js/dream_engine.js` | ○ 배포 | 꿈 모티브 엔진(UMD) |
| `index.html` | ○ | `analyzeDream` 엔진 히트 · `_cwDreamEngineBlock` · `<script src="js/dream_engine.js">` |
| ★`_v788_work/p11_dream_engine_eval.js` · `p11b_dream_dom_smoke.js` · `patch_index_b.py` | ○(-f) | 평가셋 29 · jsdom 스모크 8 · index.html 패치 스크립트(멱등 · 재적용 시 already patched) |
| ★`_v788_work/p12_daily_variance_eval.js` | ○(-f) | variance 평가 11 |
| `js/chat.js` · `api/fortune.js` | ○ | variance 키 |
| `eval/eval_dream_daily_guard.js` · `eval/_gate_pins.json` · `tools/run_gate.js` | ○ | PROMPT_KEYS_KNOWN · pin 2건 · GATE_PINS_SHA256 · 패키지 후보 경로 |
| `tools/sync_gate_pkg.js` | ○ | 패키지 후보 경로 |
| ★`_v786_diag/_ip_face_root.js` | ○(-f) | 관상 IP 해석기 |
| `_v786_diag/{compile_face_rules,p04,p05,p06,p07,p08,p10}.js` | ○ | 해석기 사용 |
| ★`D:\ChunWoon_IP\{README.md,gate_pkg,face}` | **git 밖** | IP 정본 |
| `backup_pre_v788/{_gate_pkg,IP_face}` | ✗ | 이관 전 사본 |
| `_v788_work/{fortune.js,chat.js}.pre_c` | ✗(무시) | P-786-C 패치 전 |
| `천운_관상법무자문_*_2026-09-21.md` 2건 | ✗(무시) | R2 패킷 |
| 인수인계 | ○(-f) | 이 문서 |

---
## 9. ★확정 수치 대장 (재측정 불필요)
| 항목 | 값 |
|---|---|
| 꿈 엔진 | 모티브 12 · PATTERNS 12(마스크 5종) · p11 29/29 · p11b 8/8 · index.html +2,774B |
| variance | 문자열 189자(상한 400) · p12 11/11 · system 프롬프트 524B(변주 포함) |
| pin | `_gate_pins.json` sha `a1362f2c70eb1a1f…` · fortune.js 206,983B(bytes_min 117,252) · dream_daily_guard checks_min 48 |
| 이관 | gate_pkg 550파일 42MB sha 550/550 · face 12파일 364K sha 12/12 · 복사 19s |
| 게이트(이관 후) | tools 3/3 · eval 57/57 · mut 13/13 · p04 191 · p05 46 · p06 77 · p07 8 · p08 36 · p09 20 · compile 100규칙 동일 바이트 |
| 미배포 | 20커밋(이 문서 479e8ce + 참조 정정) |

---
## 10. 한 문단 요약
v787 이 남긴 「지금 가능」 두 소품과 이관을 끝냈습니다. **P-786-B**: 선언만 되고 한 번도 불리지 않던 꿈 `DREAM_KEYS` 12항목을 결정론 엔진(`js/dream_engine.js`)으로 가동해 LLM 위에 얹고 LLM 실패 시 주 풀이로 삼았으며, 한국어 부분 문자열 오탐(사용·동물·불안·계산·팥죽·불꽃)을 평가셋 29건으로 못박았습니다. **P-786-C**: `personaTone` 에 실려 가던 결정변수를 전용 키 `variance` 로 분리하고 서버가 별도 줄로 보간하게 했으며 pin 2건을 수동 갱신했습니다. **P-786-H**: 게이트 패키지와 관상 IP 를 `D:\ChunWoon_IP` 로 옮기고(sha 전건 일치) 리포가 env 없이 형제 폴더를 찾도록 해석기를 두었습니다. 이관 후 전 게이트 재측정 tools 3/3 · eval 57/57 · mut 13/13. ADR-FACE-001 은 결재안과 법무 자문 패킷까지 준비했으나 **제이 회신이 없어** 플래그는 그대로이고, push 도 미승인이라 **20커밋(이 문서·참조 정정 포함)이 로컬에 쌓여 있습니다**. 다음 세션은 두 폴더를 함께 연결해야 합니다.

---
## 11. ★2026-09-22 추기 — push·배포·브라우저 실기 (재수행 불요)
- **push 완료(제이 실행 · 2026-09-22)**: `3c917e3..a8f24b1 main -> main` (20커밋 · 247 objects) → Vercel 자동 배포 · `chunwoon.app/js/dream_engine.js` 200 (v788 헤더). ★샌드박스 `git push` 는 여전히 불가(자격증명 없음 · 결정 75) · Windows PowerShell 원격조작도 터미널은 **클릭 전용 등급**이라 입력 불가 실측 — **푸시는 제이 몫 확정**.
- ★`chunwoon.app` 은 클라우드 컨테이너·VM 양쪽 egress 403/56 — **실기는 Claude 내장 브라우저(Claude_Browser · site 허용 등록됨)로만** 가능. 카메라 캡처는 패널에서 차단(파일 업로드만).
- 세션 시작 게이트 재측정(이관 상태): sync drift=0 · p11 29/29 · p12 11/11 · p08 36/36 · tools 3/3 — 인수인계와 일치.
- **P-788-C 꿈 엔진 실기 ✅** (배포본 `analyzeDream` 직접 호출):
  「큰 돼지가 집으로 들어왔다」→ LLM 길몽 88점 + 📖 전통 해몽 사전 풀이·🐷 돼지·길·숫자 4개(LLM 아래) 11.2s / fetch 차단 재현 → ⚠️ 오류 박스 **위에** 사전 풀이 「(AI 없이 사전 대조)」 ✓ / 「회사에서 회의를 했다」→ 평몽 58점 · 사전 블록 없음 ✓ / dream fetch ctx 키 10종(categories·dayPillar·dominantElement·emotion·ilgan·ilganElement·sajuLinked·story·time·weakElement)에 engineHits·engineMotifs **없음** ✓ / `_cwDailyVariance()` 186자 ✓
- **P-787-C 관상 센서 실기 ✅** (제이 사진 1장 · 2회):
  콘솔 `[관상] AI landmark classification` 정상 · `[관상 센서] color:ok · texture:G7 거부(초점 0.387 < 0.55 · 흐린 사진 — 게이트 의도대로) · axes 106 · unreliable []` / `photoState.face.sensor.measures` 106키 **전부 col_/tex_** · meta{version 1.0.0 · axesEmitted 106} / fetch 본문 `{type:'face', features:{…sensorMeasures, sensorMeta}}` **8,008B · aiFrame 없음** ✓ (★본문 형상은 `body.features.*` — `body.ctx` 아님). 텍스처 ok 실측은 선명한 사진으로 재시도 시 가능(선택). 서버 `[cw:facever]` 로그는 Vercel 측이라 미열람.
- 남은 것 갱신: ~~push·배포~~ ~~P-787-C~~ ~~P-788-C~~ 완료 → 다음은 **ADR-FACE-001 결재 → R1·R2 → P-787-A → P-787-B → 보류(package-lock·v7.71 zip·P-786-A/E/F/G1·P-788-A)**.
- **ADR-FACE-001 결재 완료(2026-09-22 · 제이 「A안 제안대로」)**: ADR 문서 상태 ✅ · §7 결재 결과 추가. 방향 A(5건) · R3 100% · R5 고지문 확정. ★플래그는 R1(영인)·R2(법무 회신) 충족 전까지 false — **다음 세션은 결재를 다시 묻지 않는다.** 남은 것: R1 파일 수령 · R2 패킷 발송(제이) → 그 후 켜기 절차(ADR §7).
- **R2 법무 자문 — 제이 결정(2026-09-22): 런칭 전 전 트랙 일괄 수행.** 관상 단독 발송 없음. 다음 세션은 R2 발송을 재촉하지 않는다.
- **R1 영인 수령 준비**: `D:\ChunWoon_IP\face\sources\영인_수령_안내_SRC_XLHZ_A_2026-09-22.md` + `영인_직접링크_SRC_XLHZ_A.txt`(Commons 직접 URL 10건 · dir/파일명/bytes/sha1 = source_editions.json volume_map 기준). ★VM·컨테이너 모두 commons/upload.wikimedia.org egress 차단 실측 — 제이가 자기 브라우저로 내려받아 같은 폴더에 저장 → Claude 가 sha1 대조 + sha256 등재(`scan_file_sha256`·`acquisition_status`).

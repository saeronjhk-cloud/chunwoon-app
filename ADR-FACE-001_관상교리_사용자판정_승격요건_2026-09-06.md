# ADR-FACE-001 — 관상 교리를 「사용자 판정」으로 승격하는 요건

> **상태**: ✅ **결재 완료 — 제안 A 채택 (제이 · 2026-09-22 · 「A안 제안대로」)** · 플래그는 R1·R2 충족 전까지 false 유지
> **작성**: 2026-09-06 · v786-V
> **관련**: 인수인계 v786 §5-b·5-c·5-d · `api/_engine/facever.js` · `_v786_diag/IP_face/`
> **선례**: 손금 트랙 ADR-001 · ADR-002 · ADR-003 (같은 결재 관행)

---

## 1. 무엇을 결정하는가

관상 판정 엔진은 규칙 100건을 **3층**으로 나눕니다.

| 층 | 뜻 | 현재 |
|---|---|---|
| **VERDICT** | 엔진이 확정한다. LLM 이 뒤집지 못한다 | 4건 (tier B 면 5건) |
| **REFERENCE** | 계측값 + 전통 관점. **판정이 아니다** | 23건 |
| **WITHHELD** | 내보내지 않는다 | 73건 |

★그런데 지금 **VERDICT 는 사용자에게 나가지 않습니다.**
`face_rules.json` 의 두 교리가 `user_output_enabled: false` 이고,
`api/fortune.js` 가 그 플래그를 존중해 VERDICT 를 통째로 비우기 때문입니다.

⟹ ★**이 ADR 은 그 플래그를 언제 `true` 로 올릴 것인가를 정합니다.**
⟹ ★플래그 하나만 바뀌면 판정 상품이 켜집니다. **코드 수정이 필요 없습니다.**
   (게이트 `p08` I-4·I-6 이 이 구조를 강제합니다)

---

## 2. 왜 지금은 false 인가 — 미충족 요건

| # | 요건 | 현재 | 막는 것 |
|---|---|---|---|
| ★R1 | **영인 실물 확보** — `scan_file_sha256` | ★**null** | 파일 수령 3경로 전부 실패(curl 403 · web_fetch 바이너리 미반환 · 브라우저 저장경로 없음). ★열람은 했고 판면 검증도 했으나 **바이트를 손에 넣지 못했다** |
| ★R2 | **법무 검토** — `license_review_status` | ★**UNREVIEWED** | 우리는 변호사가 아니다. LQF1(1925 秦愼安 校勘 계열) 회피는 판면으로 입증했으나 검토 자체가 없다 |
| ★R3 | **평가셋 통과 기준** | ★**기준 미정** | GT 26+35케이스가 있고 191/191 통과하나, 「몇 % 이상이면 판정으로 내보내도 되는가」를 정한 적이 없다 |
| ★R4 | **모집단 등급** | ★**TIER-C(합성)** | POPULATION 규칙은 실사진 모집단 없이는 참·거짓을 못 낸다 (P-786-A) |
| R5 | 오판정 시 사용자 고지 문구 | 미작성 | 「판정」이라 말하는 순간 책임 수준이 올라간다 |

---

## 3. 제안 — **부분 승격**

★전부-아니면-전무가 아니라, **요건을 충족한 규칙만** 켜는 것을 제안합니다.

### 제안 A (권장) — TEXT 규칙만 먼저 켠다
```
조건: threshold_origin === 'TEXT' 이고 condition_coverage === 'FULL'
현재 해당: 5건 (R016 三停皆稱 · R020/R021 눈썹↔눈 · XLHZ R014/R015 火形 상하)
근거: ★이 5건은 원문이 **비교 대상을 스스로 지정**한다.
      「눈썹이 눈보다 길다」는 모집단이 없어도 참·거짓이 정해진다.
      ⟹ R4(모집단 TIER-C)가 이 5건에는 **적용되지 않는다**.
남는 요건: R1(영인) · R2(법무) · R3(평가 기준) · R5(고지 문구)
```

### 제안 B — POPULATION 까지 켠다
```
조건: 위 + condition_coverage === 'FULL' 이고 모집단 tier ∈ {A,B}
현재 해당: +3건 (R028 눈 작음 · R030 코 작음 · R031 입 작음. 이 중 2건은 BLOCKED)
★선결: P-786-A 실사진 모집단. 생체인식정보 동의 절차가 필요하다.
```

### 제안 C — 켜지 않는다 (현행 유지)
```
REFERENCE 23건만 나간다. 「계측값은 이렇고, 전통 관상학은 이렇게 봅니다」.
★이것이 「계측 엔진」 포지셔닝이며, 지금 상태로 출시 가능하다.
```

---

## 4. ★결재해 주실 것

```
① 제안 A · B · C 중 무엇으로 갑니까?
② A 로 간다면 R3(평가 통과 기준)을 몇 %로 정합니까?
   ★참고: 현재 GT 평가는 「원문 서술을 넣으면 원문이 말한 판정이 나오는가」를 봅니다.
        사람 얼굴에 대한 정답이 아니라 **원문 재현 정확도**입니다.
        「관상이 맞는가」는 이 평가로 알 수 없고, 우리는 그것을 주장하지 않습니다.
③ R2(법무 검토)를 언제 겁니까? 손금 트랙처럼 자문 패킷을 만들까요?
④ R5 고지 문구 — 「판정」이라 쓸 때 어떤 한계를 함께 밝힙니까?
```

---

## 5. ★어떤 경우에도 켜지 않는 것

```
✗ WITHHELD 73건 — 어떤 승인이 나도 노출하지 않는다.
  ★특히 `R006 醜惡者㐫` 계열: 원문에 「醜惡」의 정의가 없다.
    조작적으로 정의하는 순간 그것은 원문 인용이 아니라
    ★**우리가 사용자 얼굴을 추하다고 판정하는 코드**가 된다.
    게이트 p08 D 가 4 tier 전부에서 미노출을 강제한다.
✗ 귀 규칙 — 센서가 없다. 게이트 p08 E 가 막는다.
✗ `condition_coverage === 'PARTIAL'` — 부분으로 전체를 판정하는 것은 원문 왜곡이다.
  게이트 p08 H-3 이 막는다.
```

---

## 6. 되돌리는 법

`api/_engine/face_rules.json` 의 `doctrines[].user_output_enabled` 를 `false` 로 되돌립니다.
★재컴파일(`node _v786_diag/compile_face_rules.js`)하면 IP 원본의 값으로 덮어쓰이므로,
승인 시에는 **IP 원본(`_v786_diag/IP_face/doctrines.json`)을 고치고 재컴파일**하십시오.

---

## 7. ★결재 결과 (2026-09-22 · 제이)

```
① 방향      : 제안 A — TEXT·FULL 5건 한정 (R016 三停皆稱 · R020/R021 눈썹↔눈 · XLHZ R014/R015 火形 상하)
② R3        : 해당 5건 GT 100% (현재 충족 · p08 36/36). ★「원문 재현 정확도」이지 「관상이 맞는가」가 아님을 명기.
③ R5 고지   : 「이 항목은 고전 문헌의 서술을 계측값에 적용한 결과이며, 얼굴이나 운세에 대한 사실 판단이 아닙니다.」 (BAN_B04 회피)
④ R2 법무   : ★제이 결정(2026-09-22) — **앱 런칭 전 전 트랙(사주·손금·관상·작명 등) 일괄 자문**으로 수행. 지금 개별 발송하지 않는다. 패킷 초안 2건은 git 밖 보관 유지. 회신 → 법무/관상/회신_<패널>_<날짜>.md → 채점 → 충돌 대조 → 변호사 질문 5개.
⑤ R1 영인   : 相理衡眞 10冊 파일을 제이가 직접 수령 → D:\ChunWoon_IP\face\sources\ → sha256 등재.
```

★**채택 ≠ 켜기.** R1·R2 가 비어 있는 동안 `user_output_enabled` 는 **false 유지**. 두 요건이 채워지면:
`D:\ChunWoon_IP\face\doctrines.json` 플래그(FACE_OGWAN · 해당 5건 범위) → `node _v786_diag/compile_face_rules.js` → p08 I-6 통과 → 인수인계 기록.
§5(어떤 경우에도 켜지 않는 것)는 그대로 유효. §6 되돌리는 법의 IP 원본 경로는 이관 후 `D:\ChunWoon_IP\face\doctrines.json` 이다.

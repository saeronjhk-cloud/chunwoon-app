# [2026-09-05] 천운 — ★전 기능 「출력 다양성 붕괴」 진단·수리 인수인계 v786 ★정본

> **정본 위치**: `D:\AI ChunWoon\천운_인수인계_v786_다양성붕괴_진단수리_2026-09-05.md`
> **승계 (폐기 금지)**: 인수인계 v7.68 정본 · 손금 트랙 v7.2 정본 · ADR-001~003
> **작업 트랙**: ★손금이 아니라 **본체(사주·관상·궁합·토정·타로·꿈·작명·데일리)** 입니다.
> **진단 산출물 폴더**: `D:\AI ChunWoon\_v786_diag\`
> **패치 전 백업**: `D:\AI ChunWoon\backup_pre_v786\` (index.html · fortune.js · ctxguard.js · tarot.js · chat.js)

---

## 0. ★★★한 장 요약 — 제이가 관찰한 현상의 원인

제이 보고: **「관상 결과를 여러 명이 비교하니 대부분 비슷하게 나온다」**

★**맞습니다. 그리고 관상이 가장 심했습니다.**

| # | 사실 | 실측 |
|---|---|---|
| ★★1 | **관상 4축 라벨이 두 사용자에게 전부 같을 확률** | ★**26.1%** (사진비 혼합) · ★**60.3%** (정사각 사진만) |
| ★★2 | 「각진형」이 얼굴형의 **80.7%** · 「작은 입」이 입의 **98.8%** | 입은 4종 중 **2종만** 도달 |
| ★★3 | **「매부리코」는 수학적으로 도달 불가**했습니다 | 60,000건 표본에서 **0건** |
| ★★4 | ★**얼굴형을 결정한 것은 얼굴이 아니라 사진 비율**이었습니다 | 같은 사람을 5가지 비율로 찍어 판정이 유지될 확률 **0.18%** |
| ★★5 | 토정비결 **상괘는 같은 해 신청자 전원 동일** | 상괘 top-1 점유 **100%** · 팔괘 8종 중 **4종 영구 미도달** |
| ★6 | 궁합 점수는 clamp 로 **11.5%가 소실**, 55점 하나가 **10.6%** | raw 범위 16~125를 55~99로 잘랐음 |
| ★7 | 타로 셔플이 균일하지 않음 (`sort(()=>Math.random()-.5)`) | χ²/df **208.9** (균일=1) |
| ★8 | 데일리 메시지는 사주 없는 사용자에게 **15종**뿐 | 같은 날 충돌확률 **6.44%** |
| ★9 | 무료 4상품의 **엔진 확정 출력 필드가 0개** | 출력 100%가 LLM 자유생성 |

★**전부 수리했고, 수리 전/후를 같은 하네스로 실측했습니다.** (§4)

★★**아직 남은 것**: 관상 참조분포가 **합성 유래(TIER-C)** 입니다. 실사진 캘리브레이션이 필요합니다(§6-1).

---

## 1. ★★진단 방법 — 무엇을 근거로 말하는가

### 1-1. 검증된 사실 (verified · 가정 아님)

| ID | 사실 | 근거 |
|---|---|---|
| ★**V1** | `FACEMESH_FACE_OVAL` 순회 순서는 `10→338→297→332→284→**251**→389→**356**→**454**→323→…→**152**` | npm `@mediapipe/face_mesh@0.4.1633559619` 번들을 Node vm 으로 적재해 **실측** |
| | ⟹ **251(이마옆) > 356(관자) > 454(광대·최대폭)** 순으로 아래에 있다 | |
| | ⟹ ★코드의 `jawW = |x356 − x127|` 는 **턱이 아니라 관자놀이 폭**이다 | 실제 하악각은 **172/397** |
| | ⟹ 볼록 윤곽에서 `foreheadW(21↔251) < jawW(127↔356)` 가 **구조적으로 성립** ⟹ 「역삼각형」 분기 도달 불가 | |
| ★**V2** | `canonical_face_model.obj` 정점 1~3(=LM0,1,2): LM1(코끝)=(0,−1.126865,7.475604), LM2=(0,−2.089024,6.058267) | google-ai-edge/mediapipe 원본 |
| | 모델은 +y 가 위 ⟹ 이미지좌표(+y 아래)에서 **`ai[1].y − ai[2].y < 0` 이 항상 성립** ⟹ ★「매부리코」 분기 **수학적 도달 불가** | |
| ★**V3** | `index.html:4941-4943` — FaceMesh 입력 캔버스가 **사진 종횡비를 보존**한다 | 코드 직독 |
| | MediaPipe 는 x 를 **폭**, y 를 **높이**로 각각 정규화 ⟹ x/y 를 섞는 비율(`whRatio`·`eyeRatio`)은 **사진 비율에 비례해 왜곡**된다 | |

### 1-2. 가정 (assumption · 반드시 sweep 으로 검증)

관상 합성 모집단의 **중심값**은 canonical 모델 앵커 + 통상 안면계측값에서 잡았고, **분산(CV)은 미지**입니다.
⟹ **CV 를 3%~25% 로 흔들어도 유지되는 결론만** 주장했습니다.

```
CV     shape top1        eyes top1      nose top1      mouth top1     4축조합 고유
 3%    square 100.00%    big 98.91%     high 100.00%   small 100.00%    3/256
 5%    square  99.96%    big 93.99%     high  99.60%   small 100.00%    7/256
 8%    square  98.08%    big 82.53%     high  94.94%   small  99.78%   15/256
12%    square  90.40%    big 70.11%     high  84.72%   small  97.29%   46/256
18%    square  76.74%    big 56.69%     high  72.09%   small  89.83%   87/256
25%    square  64.35%    big 46.42%     high  61.85%   small  83.61%  125/256
```
★**비현실적으로 큰 CV 25%에서도 입은 83.6%가 「작은 입」입니다.** 결론은 분산 가정에 견고합니다.

### 1-3. ★측정하지 않은 것 (정직한 한계)

| 항목 | 왜 안 했나 |
|---|---|
| ★**LLM 출력 문자열 자체의 유사도** | Anthropic API 실호출 비용·부작용. temperature 미설정(기본 1.0)·캐시 없음이므로 **문자열은 호출마다 다릅니다**. 「비슷하다」의 구조적 원인은 **프롬프트 결정변수 부족 + 엔진 앵커 0** 으로 한정해 보고합니다. |
| ★실사용자 중 사주 미보유 비율 | 서버 로그·분석 데이터가 저장소에 없음 |
| ★실제 얼굴 사진 기반 랜드마크 | 사진 데이터 없음. **§6-1 이 이걸 해소하는 유일한 경로입니다.** |
| 작명 추천 이름 집합의 성씨별 겹침률 | 이름 후보를 만드는 건 서버 엔진이 아니라 LLM. 유료 호출 20건×N회 필요 |

---

## 2. ★★★관상 — 3대 결함

### 2-1. 결함 ① 사진 종횡비가 관상을 결정했다 (가장 심각)

`whRatio = faceW(x축) / faceH(y축)` 인데 MediaPipe 는 x 를 사진 폭, y 를 사진 높이로 **각각** 정규화합니다.
⟹ 같은 얼굴이라도 사진 비율이 바뀌면 `whRatio` 가 그 비율만큼 통째로 이동합니다.

```
사진비 A=H/W   대표          shape 분포                    eyes 분포
 0.750  가로 4:3    long 93% square 7%            big 47% round 39% droopy 14%
 1.000  정사각      square 98% long 2%             big 82% droopy 12% round 6%
 1.333  세로 3:4    square 100%                   narrow 57% big 34% droopy 9%
 1.500  세로 2:3    square 100%                   narrow 85% droopy 8% big 7%
 1.778  세로 9:16   square 100%                   narrow 93% droopy 6%
```
★**같은 사람을 5가지 비율로 찍었을 때 4축 판정이 전부 같을 확률: 0.18%.**
★즉 「관상」이 아니라 **「사진 촬영 비율 판독기」** 였습니다.

### 2-2. 결함 ② 랜드마크 오지정 — 턱을 재지 않았다

```js
const jawW = Math.abs(ai[356].x - ai[127].x);   // 주석: "LM 127 = right jaw"
const jawRatio = jawW / faceW;
if (jawRatio > 0.88) shapeOpt = square;
```
V1 에 의해 127/356 은 **관자놀이**입니다. 관자폭/광대폭 ≈ **1.0** 이므로 `>0.88` 이 거의 항상 참
⟹ ★**「각진형」이 80.7%** 를 차지했고 「둥근형」은 0.1%, 「역삼각형」은 0.0% 였습니다.
(실측 `jawRatio` p05~p95 = **0.948 ~ 1.053**. 임계 0.88 은 분포 **바깥**입니다.)

### 2-3. 결함 ③ 임계가 어떤 모집단으로도 보정된 적이 없다

| 분기 변수 | 실측 p05 / 중앙 / p95 | 코드 임계 | 결과 |
|---|---|---|---|
| `whRatio` | 0.704 / 0.810 / 0.917 | 0.85(wide) · 0.68(long) | 사진비에 끌려감 |
| `jawRatio` | 0.948 / 1.000 / 1.053 | 0.88 · 0.85 | ★항상 참 |
| `eyeRatio` | 2.615 / 3.100 / 3.587 | 4.0(narrow) · 3.0(round) | 사진비에 끌려감 |
| `eyeSizeRatio` | 0.044 / 0.056 / 0.072 | 0.055(big) | 경계에 걸침 |
| `noseWRatio` | 0.230 / 0.265 / 0.300 | 0.30(wide) · 0.20(small) | ★대부분 미달 |
| `mouthFaceRatio` | 0.696 / 0.790 / 0.884 | **0.95(small)** | ★**전원 「작은 입」** |
| `tipToAlar` | −0.028 / −0.025 / −0.022 | **+0.015(hooked)** | ★**부호가 반대 — 영구 도달 불가** |

★`mouthFaceRatio` 는 **구각폭/동공간거리** 입니다. 사람의 입은 동공간거리보다 좁으므로(≈0.79) 임계 0.95 는
**모든 인간을 「작은 입」으로 만듭니다.** `lipThickness` 분기는 그 뒤에 있어 **영영 도달하지 않습니다.**

### 2-4. 점수도 붕괴

```
overallScore = 60 + symmetry*15 + thirdsScore*12 + goldenProximity*10   → clamp[65,99]
```
실측: **범위 77~95 · SD 2.41 · p05~p95 = 84~92 · 고유값 18개**
부위 점수는 더 심합니다 — ★**mouthScore 는 고유값 1개(86점, 100%)**, eyeScore 고유값 2개(90점이 98.6%),
shapeScore 는 `FACE_R.shape` 의 4개 상수(80/82/84/86)뿐이고 그중 82점이 97.7%.

---

## 3. ★기타 기능 결함 (관상 외)

| 기능 | 결함 | 실측 |
|---|---|---|
| ★**토정** 상괘 | `calcUpperGua(targetYear)` 가 **연도만** 사용. 게다가 `(y−4)%10` 과 `(y−4)%12` 는 항상 같은 패리티 ⟹ 태세수가 **항상 짝수** ⟹ `%8∈{0,2,4,6}` | 상괘 top-1 **100%** · 팔괘 8종 중 **건·리·손·간 4종 영구 미도달**(1900~2100 전수) |
| ★**토정** 중·하괘 | `lunarMonth%6` · `lunarDay%3` — 생년이 기여 안 함 | 괘 조합 **18/144** · H/Hmax 0.581 · 충돌 5.56% |
| ★**궁합** 점수 | `Math.min(99,Math.max(55,...))` 하드 clamp | raw 16~125 → 하한 9.25%+상한 2.28% 소실 · **55점이 10.63%** |
| ★**궁합** 일지 | 육합·육충 2표만 조회 | ★**83.70%가 `무관`** — 시스템 프롬프트가 "가장 중요"라 강조하는 축이 5명 중 4명에게 정보량 0 |
| ★**타로** 셔플 | `[...deck].sort(()=>Math.random()-.5)` | χ²/df **208.92**(22장) · **35.07**(78장) · 전체셀 max/min **19.35배** |
| ★**타로** 표시 | 화면 정/역이 **LLM 응답값** `rd.reversed` 기준 | 뽑은 카드와 보여주는 카드가 갈릴 수 있음 (무료·프리미엄 **양쪽**) |
| ★**데일리** | 사주 없으면 `ctxBlock` 이 빈 문자열 — 페르소나 3 × 카테고리 5 | **15종** · 같은 날 충돌 **6.44%** |
| ★**작명** 수리 | `((v-1)%81)+1` — JS `%` 는 음수를 그대로 반환 | `strokes` 없으면 idx **0** → 전 후보가 「총 0획 · 반길반흉 · ?」 동일. **한글 전용 스타일 전체가 이 상태** |
| ★**작명** 우선순위 | `llmGrade || 엔진값` · `c.soundEl || soundCheck.pattern` | 엔진 81수·발음오행 판정이 **화면에 도달 안 함**. `soundCheck.valid/conflictCount/comment` 는 참조 0건 |
| ★**엔진 결속** | `bind.computeFacts` 가 `yearPillar/monthPillar/dayPillar` 3키를 요구하는데 compat·tojeong·naming·tarot·dream ctx 에 **없음** | `computeFacts` 항상 `null` ⟹ 무료 4상품 **엔진 확정 필드 0개** |
| ★**엔진 결속 2차** | `CW_ENGINE_TYPES = ['saju','saju_premium_1','saju_premium_2']` | 3키를 실어도 `cwEng` 자체가 null 이라 호출 안 됨 |
| **꿈** | `DREAM_KEYS`(12항목) 선언 후 **참조 0건** | 매칭률 0% |
| **LLM 파라미터** | `temperature`·`top_p`·`seed`·`cache_control` **전부 미설정** · `effort:'low'` | 캐시 없음 ⟹ 「비슷함」은 캐시 탓이 아님 |

### ★양호했던 것 (다시 파지 마십시오)
```
✅ 사주 4기둥 문자열 — 43,680/43,680 전건 유일 (H/Hmax 1.000)
✅ 사주 오행 벡터 674종 · 십성 3위치 1,100종 — 붕괴 없음
✅ 타로 카드 **조합** 다양성 — 1540/1540 도달, 두 사용자 충돌 0.0120% (편향은 위치별 빈도에만 있었음)
✅ 작명 81수리 판정기·발음오행 판정기 자체 — H/Hmax 0.96 / 0.92 (엔진은 멀쩡, 화면에 연결이 안 됐을 뿐)
```

---

## 4. ★★★수리 내역과 전/후 실측

### 4-1. 관상 — `index.html` 코어 전면 교체 (구 243줄 → 신 213줄)

★**정본은 `_v786_diag/face_core_v786.js`** 입니다. `index.html` 안의 것은 참조분위수를 주입한 **빌드 산출물**입니다.
⟹ 관상 로직을 고칠 때는 **정본을 고치고 `node _v786_diag/p03_build_and_eval.js` 로 재빌드**하십시오.

| # | 수리 |
|---|---|
| ① | **등방화** — `photoState.face.aiAspect = tmpCv.height/tmpCv.width` 를 저장하고 계측 전에 `y *= A` |
| ② | **랜드마크 정정** — `jawW` 를 127/356(관자) → **172/397(하악각)** |
| ③ | **고정임계 → 참조분위수** — 24축 전부 `CW_FACE_REF.q[축]` 의 분위수 랭크로 판정. 임계 상수 코드 산재 금지 |
| ④ | **매부리코 실제 판정** — 콧대 볼록도(LM168→LM1 현 대비 6/197/195/5 의 돌출)를 새 계측축으로 신설 |
| ⑤ | **점수 재정규화** — 분위수 랭크를 62~97 에 균등 사상 |
| ⑥ | **계측축 12개 승격** — 이미 재고 있던 십이궁·오관(명궁·천이·자녀·질액·전택·눈썹4·관골·인중·지각)을 분류축으로 사용 |
| ⑦ | **구 명칭 별칭 유지** — `eyeRatio`·`mouthFaceRatio`·`thirdsScore`·`goldenProximity` 등 기존 화면·프리미엄 렌더러 호환 |
| ⑧ | **프롬프트 결정변수 주입** — `api/fortune.js` 에 `cwFaceAxisBlock()` + `CW_FACE_AXIS_RULE` 신설. face·face_premium_1·face_premium_2 3곳에 **24축 백분위**를 싣고 「최소 6개 축의 백분위를 근거로 서술 · 일반론 금지」를 강제 |

**실측 (평가 20,000건 · CV 5/8/12% × 사진비 0.75~1.778 · 캘리브와 독립 시드)**

| 지표 | 현행 | **v786** |
|---|---|---|
| ★두 사용자 4축 전부 동일 확률 | **26.14%** | ★**0.63%** (41배 개선) |
| 4축 조합 고유 | 46종 | ★**256종 / 256** (이론 최대) |
| 정규화 엔트로피 | 2.58 bit | ★**7.59 bit** (max 8) |
| shape 분포 | square **80.7** / long 19.1 / round 0.1 / inv **0.0** | 28.6 / 25.1 / 24.2 / 22.2 |
| eyes 분포 | narrow 46.8 / big 34.4 / droopy 10.4 / round 8.4 | 34.7 / 22.9 / 21.7 / 20.6 |
| nose 분포 | high **93.4** / wide 6.2 / small 0.4 / hooked **0** | 49.3 / 31.5 / 12.5 / **6.6** |
| mouth 분포 | small **98.8** / thick 1.2 / (2종 미도달) | 27.6 / 24.3 / 24.1 / 24.0 |
| 종합점수 | 77~95 · SD 2.41 · 고유 18 | 64~93 · SD **4.42** · 고유 30 |
| ★사진 종횡비 불변성 | **0.18%** | ★**100.00%** |
| ★24축 시그니처 | 없음 | **20,000/20,000 전건 유일** (H 14.29 bit) |

### 4-2. 토정 — `index.html` + `api/_engine/ctxguard.js` **동시** 수리

구 `calcUpperGua`/`calcMiddleGua`/`calcLowerGua` **삭제**(사본 금지), `calcTojeongGua()` 신설.
```
상괘 = tjMod(태세수 + 행년수(세는나이), 8)
중괘 = tjMod(상괘수 + 월건간지수 + 음력월, 6)
하괘 = tjMod(중괘수 + 일진간지수 + 음력일, 3)
```
월건·일진은 **`CW_ENGINE.recompute` 산출**을 씁니다. 못 읽으면 **null 반환 — 임의값으로 때우지 않습니다.**
★서버(`ctxguard.tojeongDerive`)가 **같은 엔진·같은 식**으로 재유도합니다.

| 지표 (N=8,736 · 1980~2005년생 × 12월 × 28일 전수) | 전 | 후 |
|---|---|---|
| 상괘 도달 | **1/8종** (top-1 100.00%) | ★**8/8종** (top-1 15.17%) |
| 괘 조합 고유 | 18 / 144 | ★**144 / 144** |
| 조합 top-1 | 5.79% | **1.58%** |
| H/Hmax | 0.581 | ★**0.972** |
| 두 사용자 충돌 | 5.56% | **0.87%** |
| ★클라↔서버 대조 | — | ★**불일치 0 / 8,736** |

★**짝수 편향의 진짜 원인**: 생년 간지수도 항상 짝수라 **행년(나이)을 넣어야만** 8괘에 닿습니다. 실측 확인.

### 4-3. 궁합

- 하드 clamp 제거 → **로지스틱 사상 40~98**. 등급 임계를 실측 분위수(p95/p80/p50/p20)로 재설정.
- `analyzeIljiRelation` 신설 — 육합·반합·육충·삼형·무례지형·원진·육해·파·동일지지·동일오행·오행 상생상극(우선순위 표).
- `index.html:3641` 점수·등급을 **엔진 우선**으로 뒤집음.
- `ctxguard.js` 의 `COMPAT_SCORE_MIN/MAX` 55/99 → **40/98** (안 옮기면 서버가 하위 20% 점수를 폐기), `ILJI_RELATIONS` 7종 → **80종**.

| 지표 (4,000명 풀 · 무작위 300,000쌍) | 전 | 후 |
|---|---|---|
| 점수 범위 / 고유값 | 55~99 / 45 | **40~98 / 51** |
| 단일 버킷 top-1 | 55점 **10.63%** | 87점 **3.53%** |
| clamp 발동 | 하한 9.25% + 상한 2.28% | ★**0%** |
| `iljiRelation` **`무관`** | **83.70%** (고유 7종) | ★**0.00%** (고유 30종) |

### 4-4. 타로

- `_cwShuffle()` (Fisher-Yates) 신설, `sort(()=>Math.random()-.5)` 교체. 역방향 확률 상수 `CW_TAROT_REVERSED_P=0.45` 로 통합.
- `_cwCardTruth()` 신설 — ★**클라가 뽑은 `c.n`·`c.reversed` 가 정본**. LLM 값은 표시에 미사용, 불일치 시 `console.warn`. **프리미엄 경로에도 같은 결함이 있어 함께 수리**.

| 덱 | 셔플 | χ²/df | 위치0 max/min | 전체셀 max/min |
|---|---|---|---|---|
| 메이저22 | 전 `sort` | **208.92** | 4.13배 | 12.89배 |
| 메이저22 | 후 F-Y | **1.004** | 1.06배 | 1.08배 |
| 풀덱78 | 전 `sort` | **35.07** | 5.58배 | 19.35배 |
| 풀덱78 | 후 F-Y | **1.019** | 1.20배 | 1.23배 |

### 4-5. 데일리 메시지

`_cwTodayIljin()` · `_cwNowSijin()` · `_cwSeedBucket()` · `_cwDailyVariance()` 신설.
★**설계 제약**: 새 ctx 키를 만들 수 없었습니다 — `api/fortune.js` 의 `daily_message` 분기가 읽는 키가 고정이고,
게이트 `eval_ctx_key_surface.js` K-2 가 「클라가 싣는데 서버가 안 읽는 키」를 dangling 으로 FAIL 시킵니다.
⟹ 서버가 이미 보간하는 **`personaTone`** 에 실었습니다. **차선책입니다** (§6-3 에 정식 경로).

| 축 | 카디널리티 |
|---|---|
| 페르소나 × 카테고리 | 15 (수정 전 **전부**) |
| 일진(60갑자) | 60 — 날짜축(같은 날 충돌엔 기여 0. 정직하게 분리) |
| 시진(12지시) | 12 |
| 성향 버킷 | 32 (localStorage 로컬 시드 → 버킷 인덱스만 사용) |
| 요청 회차 | 3 |
| **같은 날 조합** | 15×12×32×3 = ★**17,280** |

| 시나리오 | 충돌확률 |
|---|---|
| 수정 전 | **6.44%** |
| 수정 후 (요청시각 균일) | ★**0.01%** |
| 수정 후 **최악** (같은 시진·같은 회차) | 0.075% |

★일진 산출식은 서버 `api/_engine/manse.js:dayGanzi` 와 **1900-01-01~2050-12-31 전건 55,152일 대조 — 불일치 0**.

### 4-6. 작명

- `((v-1)%81+81)%81+1` 로 수정 + `v<=0`·표 결번은 ★**UNMEASURED(판정 불가)** 명시. 임의값 날조 금지.
- `gradeLabel`·`soundEl` 을 **엔진 우선**으로 뒤집고, `soundCheck.valid/conflictCount/comment` 를 화면에 노출.

### 4-7. 엔진 확정값 결속

- `index.html` 의 tojeong·dream·naming(무료+프리미엄 4슬롯) ctx 에 `yearPillar/monthPillar/dayPillar` 3키 적재. ★없으면 **키를 만들지 않습니다**(날조 금지).
- `ctxguard.js` 에 `ENGINE_PILLAR_KEYS` 신설 + `guardPersonContext(opts.extraKeys)`. ★상품별 `*_CTX_KEYS` 표에 넣지 **않은** 이유: 그 표는 「프롬프트 보간 키와 1:1」이 계약이고 게이트가 `fortune.js` 소스에서 재추출해 대조합니다. 3키는 **엔진이 읽는 키**라 성질이 다릅니다.
- `api/fortune.js` 에 ★**`CW_FACTS_ONLY_TYPES`** 신설 — tojeong·naming(3)·tarot(2)·dream(2) 에서 `computeFacts`/`applyEngineFacts` 를 활성화. ★`CW_ENGINE_TYPES` 에 넣지 않은 이유: 그 상수는 「원국 재계산 + 400 차단」의 대상이라, ctx 형상이 다른 type 을 넣으면 **전 요청이 400** 이 됩니다(v7.73 관통 #8 재발).
- ★**compat 은 제외**했습니다: `bind.js` 의 사실 블록은 **주체 표기가 없는 1인용**이라, 2인 상품에 한 사람의 십성·대운을 실으면 「두 사람의 확정값」으로 읽힙니다.

---

## 5. ★★회귀·게이트 상태

### 5-1. 통과 확인 — ★게이트 16종 466검사 전건 통과

| eval | 결과 | | eval | 결과 |
|---|---|---|---|---|
| `eval_ctx_birth_keys` | ★**24/24** | | `eval_dream_daily_guard` | 48/48 |
| `eval_tojeong_guard` | ★**21/21** | | `eval_client_port_drift` | 10/10 |
| `eval_ctxguard` | 58/58 | | `eval_lunar_ui_dom` | 21/21 |
| `eval_ctx_discard_surface` | 36/36 | | `eval_policy_binding` | 12/12 |
| `eval_ctx_dead_value` | 10/10 | | `eval_port_isolation` | 10/10 |
| `eval_ctx_key_surface` | 11/11 | | `eval_tmp_hygiene` | 9/9 |
| `eval_compat_guard` | 46/46 | | `eval_token_roundtrip` | ★**51/51** |
| `eval_naming_tarot_guard` | 57/57 | | `eval_naming_company_guard` | 42/42 |

★**남은 실패 2건은 전부 기존 결함이거나 정상 상태입니다.**

| eval | 결과 | 사유 |
|---|---|---|
| `eval_engine_binding` | 20/21 | ★**기존 결함** — 패키지 `engine/` 정본 부재(`authority.js`·`daewoon.js`·`manse.js`·`myeongli.js`). 이번 작업과 무관 |
| `eval_gate_asset_commit` | 10/11 | ★**정상** — A-4 는 「커밋 blob == 워킹 바이트」 검사입니다. **커밋 전이라 당연히 붉습니다.** 커밋 후 재실행하면 해소됩니다 |
| `eval_response_scrub` · `eval_prompt_citation_guard` | ★**실행 불가** | `eval/_failclosed.js` 가 리포에 없음(`git ls-files` 미등재). 낡은 핀이 아니라 **파일 결손** ⟹ §6-4 |

| 그 밖의 검사 | 결과 |
|---|---|
| `index.html` 인라인 스크립트 문법 | `SCRIPT#2 len=453` **1건만** (JSON-LD 블록 — ★**패치 전에도 동일**. 기준선) |
| `node --check` — fortune.js · ctxguard.js · tarot.js · chat.js · run_gate.js · eval 2종 | 전부 통과 |

★★**주의 — 패치 도중 게이트가 실제로 두 번 붉어졌고, 둘 다 제 변경이 원인이었습니다. 기록으로 남깁니다.**
```
① eval_ctxguard R-2 「guardContext 는 computeFacts 앞에서 호출된다」
   → 제가 `cwEng.computeFacts(context)` 를 `cwFactsEng.computeFacts(context)` 로 바꿔
     게이트의 리터럴 대조가 깨졌습니다.
   ★해결: 게이트를 고치지 않고 **코드를 되돌렸습니다** — `const cwEng` 를 `let cwEng` 로 바꿔
     같은 핸들에 늦게 엔진을 물립니다. 리터럴이 보존되므로 게이트는 무변경입니다.
   ★교훈: 게이트가 리터럴로 대조하는 지점은 변수명을 바꾸지 마십시오.
② eval_ctx_discard_surface E-0 「CW_*_TYPES 집합 일치」
   → 제가 만든 `CW_FACTS_ONLY_TYPES` 에 **실재하지 않는 type** (`tarot_premium`·`dream_premium`)을
     넣었습니다. 실제 이름은 `tarot_premium_1/2`·`dream_premium_1/2` 입니다.
   ★해결: 무료 4종(`tojeong`·`naming`·`tarot`·`dream`)으로 한정. 프리미엄 확장은 P-786-F 로 남깁니다.
   ★교훈: ★게이트가 이 오타를 잡았습니다. 이 게이트는 살려 두십시오.
```

### 5-2. ★핀 갱신 2건 (근거를 남깁니다)

| 파일 | 갱신 | 사유 |
|---|---|---|
| `eval/eval_ctx_birth_keys.js:248` `CLOSED_PIN` | compat `0524236f…` → **`30859a89…`** · tojeong `cbba7035…` → **`b119a0ec…`** | 두 payload 가 **의도적으로** 바뀜 (§4-2·4-3) |
| `eval/eval_tojeong_guard.js:166` `CLEAN_CTX` | 태(兌)☱·진(震)☳·하(下) → ★**간(艮)☶·손(巽)☴·중(中)**, wolNum 4→14, ilNum 21→12 | 작괘식 교체. ★`index.html` 의 `calcTojeongGua` 를 Node 로 꺼내 **독립 재현** 후 기입 |
| `eval/_gate_pins.json` | 위 두 파일의 `sha256` 만 교체 | ★`checks_min` 은 **건드리지 않음**(24·21 유지). ★`regen_gate_pins.js` 를 돌리지 **않았습니다** — 돌리면 아래 침식이 통째로 지워집니다 |
| `tools/run_gate.js:64` `GATE_PINS_SHA256` | `ee4e73b4…` → **`1495ed8f…`** | pin 파일의 pin(상호 대조). 위 갱신의 귀결이며, 안 바꾸면 `eval_token_roundtrip` D1 이 붉습니다 |

### 5-3. ★★★게이트가 이미 침식돼 있습니다 (이번 작업과 무관 · 기존 상태)

★`node tools/run_gate.js` **기준선 fail=66** (패치 전에 측정, `_v786_diag/gate_baseline_2026-09-04.txt`).

| 침식 | 내용 |
|---|---|
| ★**eval 파일 39종 소멸** | `eval_metamorphic_*` 16종 · `mutation_kill_*` 13종 · `eval_naming_gyeok` · `eval_naming_phonetic` · `eval_naming_radical_table` · `eval_root_wonmun` · `eval_scrub_token_policy` · `eval_wonmun_anchor_cases` · `eval_p1auth_v767fix` · `eval_rl` · `eval_vercel_config` — pin 표에는 있는데 **파일이 없습니다** |
| ★`eval/_failclosed.js` **부재** | `eval_response_scrub.js` · `eval_prompt_citation_guard.js` 가 `require` 하는데 리포에 없음(`git ls-files` 미등재) ⟹ **두 게이트 실행 불가** |
| ★`engine/` 패키지 정본 부재 | `eval_engine_binding` SELF-1 실패 — `authority.js`·`daewoon.js`·`manse.js`·`myeongli.js` |
| ★`mutation 0 < 하한 11` · `eval 20 < 하한 35` | 위 소멸의 귀결 |
| jsdom 미설치 | ★**이번에 `npm i -D jsdom` 로 해소**. 덕분에 `eval_ctx_birth_keys`(24검사)·`eval_ctx_dead_value`(10검사)가 **실제로 판정**하게 됐습니다 |

★★**이건 다음 세션의 최우선 과제 중 하나입니다 (§6-4).** 지금 상태로는 「게이트 통과」가 보증이 아닙니다.

---

## 5-b. ★★★관상 원문 트랙 착수 (v786-b · 2026-09-05 오후)

### 5-b-0. ★왜 열었나 — 제이 지적

> 「무조건 다양한 게 좋은 게 아니라, 실제로 관상학이 반영된 것인지가 더 중요한 것 같은데?」

★**맞는 지적이었고, 확인해 보니 두 가지가 나왔습니다.**

### 5-b-1. ★관상은 IP 파이프라인을 하나도 통과하지 않았다

| 도메인 | 등재 판본 | doctrine | 규칙 JSON | 원문 직독 |
|---|---|---|---|---|
| 사주 | 6종(PD 확정) | 있음 | 7개 | 영인본 7회차 |
| 작명 | 3종 | 있음 | 5개(원획 20,992자) | 있음 |
| ★**관상** | ★**0종** | ★**0건** | ★**`rules/face` 없음** | ★**0회** |

코드의 「각진형 = 강한 의지와 추진력 · 82점」은 ★**출처가 없습니다.** 프롬프트도 「전통 관상학의 일반적 관점」이 8곳일 뿐입니다.

### 5-b-2. ★★v786-a 가 교리를 지웠던 지점 — 되돌렸습니다

관상 축은 두 종류입니다.

| 종류 | 정의 | 예 | 모집단 |
|---|---|---|---|
| ★**ABS** 자기참조 절대축 | 원문이 **기준을 스스로 준다** | 三停均等 · 左右均衡 · 五官相稱 | ★불필요 |
| **REL** 상대축 | 원문이 **정량 기준을 주지 않는다** | 準頭豐隆(코끝이 도톰) | ★필요 — 우리 몫 |

★구코드는 `symmetry`·`thirdsScore` 를 **절대값(1 − 편차)으로 올바르게** 쓰고 있었는데,
★**v786-a 가 다양성을 늘리려고 전부 분위수 랭크로 덮었습니다.** 삼정이 완벽히 고른 사람도 「상위 32%」로만 표현되고
「고르다」는 판정이 사라졌습니다 — ★교리를 지운 것입니다.

★**v786-b 수리**: `CW_FACE_AXIS_KIND` 로 ABS/REL 을 분리하고, 점수를 둘로 나눴습니다.

| 값 | 성질 | 실측 | 해석 |
|---|---|---|---|
| ★`harmonyScore` 조화도(和) | **ABS 절대값** | 62~93 · p05~p95 **76~89** · SD 3.94 | ★**좁은 것이 정상**. 억지로 벌리면 그게 왜곡 |
| ★`distinctAxes` 특징(格) | REL 상대 랭크 상위 5축 | 고유 **19,789종** · 두 사용자 동일 **0.01%** | ★사람을 구분하는 일은 이쪽이 맡는다 |

프롬프트에도 못박았습니다 — ★「[균형 계측 — 절대 기준] 블록에서는 절대로 "상위 몇 %"라고 쓰지 마라. 대부분이 비슷하게 고른 것이 정상이다.」

### 5-b-3. ★★★파이프라인 관통 시운전 결과 — 이번 세션 최대 소득

문헌 조사 16건 → `_v786_diag/face_sources_candidates_2026-09-05.json`
★**정본 1순위 = 相理衡眞 道光13년(1833) 刻本** (卷二 十二宮總訣 · 卷二 三停三才五官六府十二學堂圖 · 卷四 五官總論 · 卷六 五行形相論 — ★4대 프레임을 한 책에 전부 보유. 후보 중 유일). ★**영인 미확보.**

★시운전은 **太淸神鑑**(維基文庫 전사 · 확보비용 0 · ★영인 없어 정본 불가)으로 했습니다.
산출: `_v786_diag/IP_face/` — 판본 2 · doctrine 1 · ★**규칙 42건** · GT 26케이스 · 평가 `p04_face_doctrine_eval.js` **80/80**
(★변이 시험으로 게이트가 실제로 무는지 확인: 인용 개작·축 억지배당·음성대조군 훼손 → **fail=9 로 전건 적발** 후 복원)

★★**핵심 집계 — 이 숫자가 제이 질문의 답입니다.**

| 지표 | 값 |
|---|---|
| ★**원문이 수치 임계를 준 문장** | ★**0건 / 42** |
| `threshold_origin` = **TEXT**(원문이 비교 대상을 지정) | 7건 (17%) |
| `threshold_origin` = **POPULATION**(우리가 모집단을 정해야) | ★**25건 (60%)** |
| `threshold_origin` = NONE(계측 불가) | 10건 (24%) |
| `measurability` MEASURABLE / PARTIAL / UNMEASURABLE | **3** / 29 / 10 |
| ★★**실제로 판정 가능(MEASURABLE + 축 배당)** | ★★**3 / 42 = 7%** |

★그 3건: `R016 三停皆稱`(thirds) · `R020 眉過眼者富`(browLength) · `R021 短不覆眼者乏財`(browLength).
★TEXT 7건이 TEXT 인 이유는 하나뿐입니다 — 원문이 ★**비교 대상을 스스로 지정**하기 때문입니다(「三停皆稱」서로 · 「眉過眼」눈 · 「五嶽相朝」서로 · 「八小相應」서로).

### 5-b-4. ★★구조적 절단 — 정본을 구해도 안 풀리는 것

| 절단 | 실태 | 정본으로 해소? |
|---|---|---|
| ★**귀 랜드마크 0개** | MediaPipe FaceMesh 468점에 **귀 윤곽이 없다**. 그런데 耳는 오관의 하나(採聽官). 규칙 4건 전부 UNMEASURABLE. 「耳門之前」처럼 귀 기준 부위도 함께 소실 | ★**아니오** |
| ★**색·기색 축 0개** | 「淸·白如玉·黑如漆·光·瑩」. 太淸神鑑에 **「氣色」이 36회** 나오는데 우리는 색을 한 축도 안 잰다 | ★**아니오** |
| ★**텍스처 축 0개** | 「直理·橫理(주름결)·疵瘢·黑痣·毛逆」 — 원문 상법에서 **가장 빈번한 조건 유형** | ★**아니오** |
| ★**차원 불일치** (R039) | 「天庭·地角 上下相應」 — 원문은 비교 대상을 다 줬는데 `foreheadRatio`는 폭(/faceW), `chin`은 길이(/faceH)라 **비교 불가**. 축 설계 문제 | ★**아니오** |
| 계측되는데 분류축 아님 (R017) | `upperThirdPct` 를 재면서 24축에 안 올렸다. 「고른가」는 묻고 「긴가」는 안 묻는 구조 | 부분 |
| ★**모집단이 TIER-C** | POPULATION 25건인데 모집단이 합성 60,000건뿐 ⟹ 분위수 랭크만 낼 수 있고 「豐隆」의 참·거짓은 못 낸다 | ★**아니오** |
| 輯佚 결락 | 太淸神鑑 卷五에 **論鼻部·論耳部·論口部가 없다**(전수 검색 0건) | ★**예** |

★★**결론**: 정본 확보는 ★**인용의 질과 커버리지**를 풀지 ★**계측 가능성**을 풀지 않습니다.
후자는 ①실사진 모집단 ②귀·색·텍스처 센서 ③차원이 맞는 비교축 설계 — ★**문헌 트랙과 별개의 작업**입니다.

### 5-b-5. ★★원문이 우리 구현을 반증한 항목 — 즉시 반영

```
太淸神鑑 六極 「眼雖小 秀且長 … 反爲富貴」
  (눈이 작아도 빼어나고 길면 오히려 부귀하다)
⟹ ★크기 하나로 길흉을 가르는 현 구현은 ★원문에 정면으로 어긋납니다.
⟹ ogwan.json R032(八小) 가 R028·R030·R031 을 `overridden_by` 로 무력화하도록 등재했습니다.
⟹ api/fortune.js CW_FACE_AXIS_RULE 에 못박았습니다:
   「크기 하나만으로 길흉을 단정하지 마세요. 작아도 빼어나고 길면 오히려 좋다고 봅니다.」
```
★그리고 `R006 醜惡者㐫` 은 ★**원문에 정의가 없습니다.** 이걸 조작적으로 정의하는 순간
그것은 원문 인용이 아니라 ★**우리가 사용자 얼굴을 추하다고 판정하는 코드**가 됩니다. UNMEASURABLE 로 두고 노출 차단했습니다.

### 5-b-6. ★거짓 표기 3건 정정 (사용자에게 하던 말)

| 종전 문구 | 실태 | 정정 |
|---|---|---|
| 「눈·코·입을 포함한 **십이궁**을 살펴」 | ★실제 계측은 **8궁**(명·천이·자녀·질액·전택·부처·관록·재백) | 「**8개 부위**를 재고… 잴 수 없는 항목(귀·기색·주름결)은 판정하지 않습니다」 |
| 「木·火·土·金·水 **다섯 가지**로 분류」 | ★코드는 **4형**(土形 없음) | 「현재 4형만 판별하며 土形은 아직 판별하지 않습니다. 배당 기준은 원문 대조 전입니다」 |
| 火形 = 역삼각형 배당 | ★원문 근거 미확인 | 「원문 대조 전」 명시. 相理衡眞 卷六 확보 후 검증 |

### 5-b-7. ★법무 검토 필요 지점 (조사자 제기 · 변호사 아님)

```
★LQF1 (최대 리스크) — 麻衣·柳莊·相理衡眞·水鏡集 4종이 ★전부
   「文明書局 民國14년(1925) 秦愼安 校勘」 계열로만 손쉽게 확보된다.
   秦愼安 생몰년 미확인 ⟹ ★사주 LQ6(袁樹珊 校勘)과 동일 유형.
   ⟹ 회피법: 원각본을 쓴다 (相理衡眞 1833 · 麻衣 明刻 · 神相全編 明刻).
★LQF4 — 太淸神鑑(王朴)·玉管照神局(宋齊邱)·月波洞中記(老君)·冰鑑(曾國藩)·麻衣相法(麻衣道者)
   ★전부 저자 귀속이 부정되거나 다투어진다. 四庫提要가 王朴을 「其爲依託無疑」로 단정.
   ⟹ 저작권 문제는 없으나 ★사용자 문구에 저자명을 단정 표기하면 허위 표시다.
★LQF5 — 한국식 관상 규칙(턱 중시·눈 5할·일월각 등)은 ★출전 없이 유통된다.
   ⟹ (a) PD 한국 문헌 발굴 (b) own_method 로 자체 방법론화하고 그렇게 표시 (c) 넣지 않음 — 셋 중 택일.
   ★현행 코드의 출처 없는 문구를 「한국식」으로 정당화하는 것은 (b)를 표시 없이 하는 것과 같다.
★미조사 — ctext.org(접근 불가) · 규장각 · 한국고전종합DB · 장서각. 한국 문헌은 「없음」이 아니라 「확인 못 함」이다.
```

---

## 5-c. ★★★v786-c — 정본 확보·센서 신설·규칙 재판정 (착수분)

### 5-c-1. ★相理衡眞 1833 원각본 — **첫 정식 등재본**

| 항목 | 결과 |
|---|---|
| 판본 | 淸 道光13년(1833) 刻本 十卷 10冊 1函 · 天津圖書館藏 · Commons **NLC892** |
| 파일 목록 | ★**10冊 전건** — 파일명·면수·바이트·Commons `sha1` 기록. 총 412면 / 211,954,672 bytes |
| 冊↔卷 | ★**1:1 확정** (각 파일 `toc` 직독). 第1冊만 卷前+目錄+卷之一 |
| ★`scan_file_sha256` | ★**null** — 파일 수령 실패(curl 403 · web_fetch 바이너리 미반환 · 브라우저 저장경로 없음, **3경로 전부**) |
| 열람 | ★**성공** — Commons PDF 1920px 렌더를 브라우저로 판독 (2000·2560·3000px 은 차단) |
| ★판면 검증 | 自序 「道光十三年歲次癸巳小陽月 … 陳釗自序」 · 卷四 校閱者가 전부 저자 인척 ⟹ ★**20세기 校勘層 없음을 판면으로 확인**. LQF1(1925 秦愼安본) 회피 성립 |
| 등재 | `SRC_XLHZ_A` **CANDIDATE → SELECTED** · doctrine priority **1** · `license_review_status` **UNREVIEWED 유지** |

**직독** — `_v786_diag/IP_face/corpus/xlhz_*.md` 4편 27면 · 한자 **1,435자** · 판독 불가 **30자(2.1%)**

| 편 | 한자 | □ | 범위 |
|---|---|---|---|
| 卷六 五行形相論 | 868 | 10 | ★**전문** |
| 卷四 五官總論 | 167 | 5 | ★**전문** |
| 卷二 十二宮總訣 | 233 | 4 | 배당표 12건 + 命宮 완전 / 나머지 11宮 미판독 |
| 卷二 辨訛說 | 167 | 11 | 표제 + 첫 半葉 |

### 5-c-2. ★★★원문이 뒤집은 것 3건

```
① ★★火形 배당이 상하가 뒤집혀 있었다
   원문: 「其形銳而下豐」 · 「上尖如火之炎」  (卷六, 葉二十b~二二a)
   ⟹ ★火形 = 위가 뾰족하고 **아래가 풍만**한 형.
   ⟹ ★종전 코드는 火形을 **역삼각형(이마 넓고 턱 좁음)** 에 배당 — ★정반대였다.
   ⟹ 수리: `FACE_SOURCES.shape.inv` 에서 火 배당을 떼고 「(오행 미배당)」으로.
      같은 문단의 서로 다른 두 어구가 같은 결론을 지지(3200px 확대로 각각 확인).
   ★주의: 압축 배당 「金方木瘦水圓火尖土厚」만 인용해 역삼각형을 정당화하면 ★원문 절반만 읽은 것이다.

② ★오행 의미 문구가 이 원전에서 온 것이 아니다
   종전: 「水=원만·포용·재물복 / 木=성장·품격·지혜 / 火=열정·민첩·추진력」
   원문: ★「金為義 · 木為仁 · 水為智 · 火為禮 · 土為信」 (五常)
   ⟹ 수리: 원문 형태(方·瘦·圓·尖·厚) + 五常으로 교체.

③ ★★정면 사진 한 장으로 五形을 확정하는 것은 **원문 절차가 아니다**
   원문은 五形 다섯 조 **전부에 거동 조항**(坐·涉·行·臥) + 색(色黃·色赤氣枯)
   + 몸통(露臂露背·腰) + 살뼈(肉輕骨重) 조건을 함께 건다.
   ⟹ 수리: ★단일 라벨 확정을 **폐기**하고 `wuxing` **부합도 벡터 + 커버리지**로 전환.
      `wuxingCoverage.measured = ['형태']` / `notMeasured = ['색','몸통','살뼈','거동']`
   ⟹ ★土形은 판정 기준이 **원문에 완결돼 있는데**(「土主肥…肉輕骨重、色黃」) 우리 축에 걸리는 것이
      「肥」 하나뿐이다. ★즉 코드가 土形을 못 내는 것은 **원문 부재가 아니라 센서 부재**다.
```

### 5-c-3. ★신설 센서 2종 (독립 모듈 · 아직 코드 결속 전)

**색·기색** `_v786_diag/sensor_color.js` — 13축 · 부위 마스크 15개
★랜드마크 인덱스를 **추측하지 않았다**: npm `@mediapipe/face_mesh@0.4.1633559619` 의 패킹된
`geometry_pipeline_metadata_landmarks.binarypb` 를 파싱해 canonical 468정점을 실측
(검산: LM1=(0,−1.126865,7.475604) — synth_face.js V2 주석과 일치 · 좌우 대칭짝 오차 0.0000).

★★**조명 불변성** — 사진비 결함과 같은 유형의 실패를 색에서 반복하지 않기 위한 검사

| 변환 | 정규화 후 | 정규화 전 |
|---|---|---|
| 밝기 ±40% | **1.0%** | 41.8% |
| 색온도 따뜻/차가움 | **0.5%** | 64.0% |
| 감마 0.70/1.45 | **1.9%** | 38.4% |
| 좌우 그라디언트 | **3.2%** | 15.6% |
| 채도 ×0.6/×1.45 | **7.1%** | 44.8% |
| ★**최악** | ★**7.1%** | 64.0% → ★**9.1× 개선** |

원리: log 대립색이 노출·화이트밸런스를 대수적으로 상쇄 · 피부 σ 나눗셈이 감마·채도를 상쇄 ·
밝기축은 피부 위 1차 다항식 적합으로 조명장 제거.
평가 `p05_sensor_color_eval.js` **46/46** · 음성 대조군 9건 전부 fail-closed · 변이 5/5 검출.

**텍스처** `_v786_diag/sensor_texture.js` — 14축 · 색 센서 `REGIONS` 를 **객체 동일 참조로 import**

| 축 계열 | 실측 |
|---|---|
| 방향(直理/橫理) 판별 | 橫理 **−1.000** / 直理 **+0.940** |
| 印堂 세로선 개수 | 懸針 **1** · 川字 **3** · 紋交는 개수 산정 거부 |
| 덩어리 | 黒痣(둥긂) 1.12 vs 疵瘢(길쭉) **6.49** |
| ★해상도·초점 불변성 (방향축) | ★**2.6%** ← raw 19.4% (**7.5×**) · 정수축 8/8 유지 |
| ★세기축 강한 흐림(σ3.2) | ★**−31%** — ★**±15% 를 못 지킨다** |

★세기축 한계는 **원리적**이다: 주름(얼굴폭 0.6%)과 피부 미세결(1.2%)은 공간 스케일이 달라
두 스케일의 에너지 비가 초점에 불변일 수 없다. ⟹ 감추지 않고 **검사로 못박고**, G7 초점 게이트로
작동 범위를 막고, 승격 후보는 세기 절대값이 아니라 **방향·개수·형상 축**에 기대게 했다.
★혼동 대조군 5종(머리카락·그림자·화장경계·수염·안경테) 전부 처리 — 머리카락·수염·안경테는
**부위를 신뢰 불가로 내려 fail-closed**, 그림자·화장은 축 자체(선순도·색경계비)가 걸러낸다.
평가 `p06_sensor_texture_eval.js` **77/77** · 변이 6/6 검출.

### 5-c-4. ★신설 축 6개 (G4·G5) — 코드 결속 완료

```
G5 삼정 승격 : upperThirdPct · middleThirdPct · lowerThirdPct
   ★재고 있으면서 분류에 안 쓰던 3축. 「고른가(thirds)」만 묻고 「긴가」는 안 묻던 구조였다.
G4 차원 정합 : lowerUpperWidth(하악각폭/이마폭) · cheonJiWidth(天庭/地角 폭) · cheonJiHeight(上停/下停)
   ★결함: 「天庭·地角 上下相應」은 원문이 비교 대상을 다 주는데(TEXT 등급),
     우리 축은 foreheadRatio 가 폭이고 chin 이 길이라 ★차원이 달라 비교가 성립하지 않았다.
   ★이 3축은 **ABS** 다 — 원문이 「相應」이라는 기준을 스스로 주므로 모집단이 필요 없다.
★부수 수리: 역삼각형 판정을 `R.foreheadRatio − R.jawRatio`(★랭크 차)에서
  `m.cheonJiWidth`(★원시값)로 바꿨다. 분모가 같아 원시 비교가 성립하고,
  그러면 이 판정이 **TEXT 등급을 유지**한다. 랭크로 하면 TIER-C 모집단 의존(POPULATION)으로 격하됐다.
⟹ 분류축 24 → ★30축
```

### 5-c-5. ★★★통합 재판정 — 이번 착수의 성공 지표

`node _v786_diag/p07_promote_tally.js` → **7/7 통과**

| 등급 | 착수 전 | ★착수 후 |
|---|---|---|
| MEASURABLE | 5 | 5 |
| PARTIAL | 45 | ★**62** |
| UNMEASURABLE | ★**50** | ★**33** |

```
★승격 17건 (UNMEASURABLE 50 → 33)
★「무엇이든 잴 수 있는 것」 50% → ★67%
★measure_axis 가 null 이던 규칙 21건에 축이 생겼다
★★MEASURABLE 은 5% 그대로다 — ★원문이 컷오프를 주지 않으므로 POPULATION 이고,
   ★실사진 모집단(TIER-A/B) 없이는 「豐隆」의 참·거짓을 낼 수 없다. 이것이 P-786-A 다.
```

★★**억지 승격을 막는 검사를 넣었습니다** (p07 이 강제):
```
T-3 후보의 rule_id 가 전부 실재하는가        (유령 승격 차단)
T-4 등급을 낮추지 않는가                      (역행 차단)
T-5 ★귀 규칙은 승격되지 않았는가              — 귀 10건 전부 미승격 확인
T-6 ★「清」 규칙은 승격되지 않았는가           — 清 11건 전부 미승격 확인
T-7 ★MEASURABLE 로 오른 규칙은 threshold_origin 이 TEXT 인가
```

### 5-c-6. ★남은 UNMEASURABLE 33건 — 사유별

| 건수 | 사유 | 해소 경로 |
|---|---|---|
| 10 | ★**귀** — FaceMesh 468 에 귀 윤곽 없음 | ★P-786-G1 (별도 모델 필요) |
| 8 | 몸통·골육(腰·背·臂·肉輕骨重) — 얼굴 밖 | ★해소 불가 (얼굴 사진의 범위 밖) |
| 6 | 거동(坐·涉·行·臥) — 정지 사진 밖 | ★해소 불가 |
| 4 | ★`凹陷` — **깊이(z)**. 2D 가 보는 건 꺼짐이 만든 그림자뿐이고, 우리 대역통과 정규화가 바로 그 저주파 조명을 지운다 | 3D 필요 |
| 4 | `清·秀` — 비교 대상도 컷오프도 원문에 없음 | ★해소 불가 (배당하면 원문이 아니라 창작) |
| 3 | `氣`(氣剛·氣秀) — 색이 아님 | — |
| 1 | 길흉 형용(`惡`·`醜`) — 계측량이 아님 | ★해소 불가 |
| 나머지 | 눈썹 밀도(疎)·털(眼毫·鬍鬚)·자리 부재(眉上 전용 마스크) 등 | 부분 가능 |

★★`R006 醜惡者㐫` 은 ★**원문에 정의가 없습니다.** 조작적으로 정의하는 순간
그것은 원문 인용이 아니라 ★**우리가 사용자 얼굴을 추하다고 판정하는 코드**가 됩니다. 노출 차단 유지.

### 5-c-7. ★게이트 현황 (v786-c 시점)

| 게이트 | 결과 |
|---|---|
| `p04_face_doctrine_eval.js` (원문 인용·GT·음성대조군) | ★**191/191** · 변이 8/8 검출 |
| `p05_sensor_color_eval.js` | ★**46/46** · 변이 5/5 |
| `p06_sensor_texture_eval.js` | ★**77/77** · 변이 6/6 |
| `p07_promote_tally.js` (억지 승격 차단) | ★**7/7** |
| `eval_ctx_birth_keys` · `eval_ctxguard` · `eval_ctx_discard_surface` | 24/24 · 58/58 · 36/36 |
| `index.html` 인라인 문법 | `SCRIPT#2 len=453` 1건만 (JSON-LD · 기준선) |

---

## 5-d. ★★★v786-V — 판정 엔진 결속 (「가안으로 가되 업그레이드」의 구현)

### 5-d-0. 제이 결정
> 「우선 가안으로 가되, 원문 판정 상품으로 업그레이드하자」

⟹ ★**업그레이드를 문서 약속이 아니라 구조로** 만들었습니다.
★판정 등급을 **코드가 아니라 데이터가 결정**하게 했으므로, 근거가 강해지면 **코드 수정 없이** 승격됩니다.

### 5-d-1. 3층 구조

| 층 | 조건 | 프롬프트에서 | 현재 |
|---|---|---|---|
| ★**VERDICT** | `TEXT`+`MEASURABLE` **또는** `FULL`+`POPULATION`+모집단 tier A·B | 「엔진 판정 — 바꾸지 마세요」 · 응답 덮어쓰기 | 4건 |
| **REFERENCE** | 그 밖에 축이 있는 것 | 「참고 — 판정이 아닙니다. 단정하지 마세요」 | 23건 |
| **WITHHELD** | `UNMEASURABLE` 또는 `user_exposure=BLOCKED` | ★**내보내지 않음** | 73건 |

★제이 원칙 ⑤ 이행입니다 — **엔진이 낼 수 있는 것은 엔진이 내고, 못 내는 것만 LLM 이 맡습니다.**

### 5-d-2. ★★업그레이드 경로 — 처음엔 **막혀 있었습니다**

```
1차 구현 후 실측: refTier C → B 로 바꿔도 VERDICT 4건 → ★4건. 한 건도 안 늘었다.
원인: ★MEASURABLE ∩ POPULATION = 0건
      MEASURABLE 5건은 전부 TEXT(자기참조라 모집단 불필요)
      POPULATION 33건은 전부 PARTIAL
⟹ ★「모집단만 생기면 판정 상품이 된다」는 전제가 ★데이터로 성립하지 않았다.
```

★**진단**: `PARTIAL` 이 성질이 다른 두 가지를 뭉뚱그리고 있었습니다.

| 신설 필드 `condition_coverage` | 뜻 | 모집단이 생기면 |
|---|---|---|
| ★**FULL** | 원문 조건의 **모든 어절**을 우리 축이 덮는다. 못 내는 이유가 ★**오직 컷오프 부재**뿐 | ★**VERDICT 가능** |
| **PARTIAL** | 조건의 **일부만** 덮는다 (예: 「肉肥而色紅潤」에서 색만) | ★불가 — 부분으로 전체를 판정하면 왜곡 |
| **NONE** | 덮는 어절이 없다 | 불가 |

★규칙 100건에 이 필드를 부여했습니다: **FULL 8 · PARTIAL 34 · NONE 58**
★각 건에 `coverage_note` 로 **덮은 어절과 못 덮은 어절을 나열**했습니다 — 근거 없는 FULL 을 막습니다.

★★**결과: 경로가 열렸습니다.**
```
refTier C-SYNTHETIC → B :  VERDICT ★4건 → ★5건   (규칙 FACE_OGWAN_R030 승격)
★코드에 규칙 id 분기가 한 줄도 없습니다. 데이터만으로 승격됩니다.
★승격 대상(FULL∩POPULATION) 3건 중 2건은 user_exposure=BLOCKED 라 tier 가 올라도 안 나옵니다.
```

★★**그리고 센서를 결속하면 FULL 이 더 늘어납니다.**
지금 `condition_coverage` 판정은 **결속된 30축만** covered 로 셌습니다.
색 13축·텍스처 14축은 `binding_status: NOT_BOUND` 라 uncovered 로 셌습니다.
⟹ ★색·텍스처를 결속하는 순간 R027·R038·R046·R051 등이 NONE → FULL 후보가 됩니다.
⟹ ★**센서 결속 = 상품 업그레이드**가 구조적으로 성립합니다.

### 5-d-3. ★교리 승인 게이트 — 지금은 **꺼져 있습니다**

```
face_rules.json 의 두 교리가 user_output_enabled: false 다
  (decision_authority: NON_AUTHORITATIVE · 영인 sha256 미확보)
⟹ api/fortune.js 가 이 플래그를 존중해 ★VERDICT 를 통째로 비운다.
⟹ ★지금 사용자에게 나가는 것은 REFERENCE(참고) 23건뿐이다. = ★「가안」
⟹ ★★교리 승인이 나면 플래그 하나로 판정 상품이 켜진다. 코드 수정 불필요. = ★「업그레이드」
```
★결재 문서: **`ADR-FACE-001_관상교리_사용자판정_승격요건_2026-09-06.md`** — ⬜ 제이 결재 대기
 제안 A(TEXT 5건만 먼저) · B(POPULATION 까지) · C(현행 유지) 중 택일 + 평가 기준·법무·고지 문구.

### 5-d-4. ★배포 원문 격리

★규칙 100건에는 한자 원문이 들어 있습니다. **코드 저장소·프런트에 배포하면 안 됩니다.**
(pre-commit 훅이 실제로 차단했습니다 — 임계 「한자 10자 이상 연속 구간의 합 200자」)

```
_v786_diag/compile_face_rules.js  — IP 원문 규칙 → 배포용 컴파일 (★원문 제거)
  ⟹ api/_engine/face_rules.json (66KB · 규칙 100건)
★산출물 검사: 한자 연속 10자 구간 ★0건 · 최장 연속 3자(서지명) · 총 29자
★locus 는 전건 한글 음역 (「卷六 五行形相論」 → 「권6 오행형상론」)
   ★음역표에 없는 글자가 1자라도 나오면 ★컴파일 실패
★fail-closed: 연속 10자 구간이 1건이라도 있으면 ★파일을 쓰지 않고 종료
★인용은 서지 ID 로만 (기존 CITATION_RULE 관행)
```

### 5-d-5. ★결속 지점 (`api/fortune.js`)

| # | 위치 | 내용 |
|---|---|---|
| 1 | `cwFaceVer()` | `_engine/facever.js` 적재. `cwEngine` 과 같은 패턴 · ★적재 실패는 서비스 중단 사유 아님 |
| 2 | face 3종 분기 앞 | `evaluateFace({measures, ranks, refTier})` → 3층 산출 · `user_output_enabled` 게이트 |
| 3 | `CW_FACE_AXIS_RULE` | ★3층 취급 규칙 — 「판정 블록은 뒤집지 마라 · 참고 블록은 단정하지 마라 · 두 블록에 없는 건 만들지 마라」 |
| 4 | userPrompt 3곳 | `${cwFaceBlock}` 보간 |
| 5 | 응답 후처리 | `applyFaceVerdicts(parsed, result)` — bind.js `applyEngineFacts` 와 같은 성질 |

★`index.html` 은 이미 `measurements`·`ranks`·`refTier`·`signature` 를 3경로(무료+프리미엄 2)로 보냅니다.

### 5-d-6. ★★게이트 — 「만들어 놓고 안 부르는 것」을 막습니다

`node _v786_diag/p08_face_verdict_eval.js` → ★**33/33** · 변이 11/11 검출

| 검사 | 내용 |
|---|---|
| A | ★배포 산출물에 원문이 없다 (한자 연속 10자 0건 · 원문 필드명 5종 부재) |
| B | 규칙 수 보존 100 → 100 · rule_id 완전 일치 |
| ★C-2 | ★**업그레이드 경로가 살아 있다** — refTier C→B 로 VERDICT 4→5. ★안 늘면 FAIL |
| D | ★봉인 59건이 4 tier 전부에서 미노출. 「醜惡者㐫」 계열 4건 명시 검사 |
| E | ★귀 규칙 8건은 어떤 tier 에서도 VERDICT 가 되지 않는다 |
| F | fail-closed — measures 없거나 축 결손 시 판정 없음 · LLM 의 봉인 규칙 주장 폐기 |
| H-1~3 | ★FULL 인데 uncovered 가 있으면 FAIL · 금지 7계열 FULL 0건 · PARTIAL 은 전 tier VERDICT 0 |
| ★I-1~6 | ★★**결속 검사** — fortune.js 가 실제로 적재·호출·보간·게이트·덮어쓰기 하는가 |

★★I 계열을 넣은 이유: ★이 프로젝트에 **엔진을 만들어 놓고 부르지 않은 전례**가 있습니다 —
 `DREAM_KEYS` 12항목이 선언만 되고 참조 0건, 작명 81수 판정기가 화면에 도달하지 않았습니다(§3).

### 5-d-7. ★부수 수리

```
★p07 T-5 오탐 해소 — 종전엔 「인용문에 耳가 있으면」 승격을 실패시켰다.
  그런데 R027(白如玉黑如漆聳耳入鬢)은 ★눈 부분으로 승격된 것이고 「聳耳入鬢」은 여전히 미계측이다.
  ⟹ T-5a(조건이 통째로 귀) / T-5b(귀가 일부 — 승격 허용하되 ★FULL 금지)로 분리. p07 8/8.
★.gitignore 에 IP 원문 자산 등재 + 이관 지시(P-786-H · D:\ChunWoon_IP\face\)
★스테일 git 락 제거 — 2026-08-31 세션이 남긴 0바이트 `.git/index.lock` 이 5일간 커밋을 막고 있었다.
```

---

## 6. ★★남은 것 — 다음 세션 과제

### ★★6-1. P-786-A · 관상 참조분포 실사진 캘리브레이션 (최우선)

```
현재 CW_FACE_REF 는 tier="C-SYNTHETIC" 입니다.
⟹ 「각진형」은 이제 **「하악각폭이 상위 38%」** 라는 뜻이지 「실제로 각진 얼굴」이 아닙니다.
⟹ 다양성은 해결됐지만 **절대적 의미는 아직 보정되지 않았습니다.**

★필요한 것: 실제 얼굴 사진 200~500장의 **랜드마크 24축 계측값**(사진 원본이 아니라 비율 숫자만).
★법적 주의 — ★손금 트랙 §7-b 와 **같은 문제**입니다:
   · 얼굴 계측값은 **생체인식정보**(민감정보 · 별도 동의 대상)입니다.
   · 라이선스(CC0 등) ≠ 동의. 공개 얼굴 데이터셋도 **상업 판정용 동의는 없습니다.**
   · ⟹ 손금 원격수집 패킷과 **동일한 동의 절차**로 모으거나, Tier-C 로 두고
     「참조분포 미보정」을 화면에 명시해야 합니다.
★임시 대안(동의 없이 가능): 사용자 본인 계측값을 **브라우저 로컬에만** 누적해
   「내 지난 측정 대비」를 보여주는 자기참조 축. 서버 전송 없음.
```

### ★★★6-0. ADR-FACE-001 결재 — ★가장 먼저

```
★`ADR-FACE-001_관상교리_사용자판정_승격요건_2026-09-06.md` ⬜ 제이 결재 대기
   제안 A(TEXT 5건만 먼저 켬 · 권장) / B(POPULATION 까지) / C(현행 유지)
   + 평가 통과 기준 · 법무 검토 시점 · 오판정 고지 문구
★이 결재 하나로 「참고 상품」이 「판정 상품」이 됩니다. 코드 수정 불필요.
```

### ★★6-0b. P-786-I · 색·텍스처 센서 결속 — ★상품 업그레이드와 직결

```
센서 2종은 모듈·검증이 끝났고(46/46 · 77/77) ★결속만 남았습니다.
★결속하면 condition_coverage 가 다시 계산되어 FULL 이 늘고 ⟹ VERDICT 후보가 늡니다.
   (지금은 색·텍스처 축이 binding_status: NOT_BOUND 라 uncovered 로 셌습니다)
할 일: ① index.html 이 캔버스 픽셀을 센서에 태우고 결과를 features 에 실음
      ② facever 가 색·텍스처 축을 measures 로 받음
      ③ IP 규칙의 condition_coverage 재판정 → 재컴파일 → p08 C-2 재측정
```

### ★★6-1b. P-786-F · 相理衡眞 정본 확보 → 첫 정식 등재 (관상 원문 트랙 2단계)

```
★대상: 相理衡眞 道光13년(1833) 刻本 — 天津圖書館 / Wikimedia Commons NLC892 10冊
★확보 후 절차 (太淸神鑑 시운전과 동일 경로 · p04 가 이미 돌아간다)
   ① scan 확보 → sha256 기록 → source_editions 등재 (CANDIDATE → SELECTED)
   ② 卷四 五官總論 직독 — 官별 「捷徑 + 詩五首 + 說」 3단이 규칙 단위와 그대로 대응한다
   ③ 卷二 十二宮總訣 직독 — 지금 3회 언급뿐인 십이궁 공백을 메운다
   ④ 卷六 五行形相論 직독 — ★土形 판정 기준 · ★火形 배당 검증 (§5-b-6)
   ⑤ 卷二 辨訛說 — ★이설 기각에 원전 내부 근거를 쓸 수 있다
   ⑥ doctrine priority 1 로 승격 (지금 太淸神鑑이 3, ★1은 비워 뒀다)
★2순위 대조본: 神相全編 明刊本(Harvard-Yenching, IIIF) — 相理衡眞의 상위 계보.
   ★2판본 대조로 「陳釗 정리인가 원전인가」를 가른다 (SRC_ZPJZ_B 철회 교훈 반영)
★★그러나 §5-b-4 의 「아니오」 6줄은 이걸로 하나도 안 풀립니다.
```

### ★★6-1c. P-786-G · 계측 가능성 확장 (문헌 트랙과 **별개**)

| # | 항목 | 왜 |
|---|---|---|
| G-1 | ★**귀 계측 센서** | MediaPipe FaceMesh 에 귀가 없다. 오관 중 하나를 통째로 못 잰다 |
| G-2 | ★**색·기색 축** | 「氣色」 36회. 조명 정규화가 선결 |
| G-3 | ★**텍스처 축** | 주름결·점·흉터 — 원문에서 가장 빈번한 조건 유형 |
| G-4 | ★**차원 정합 비교축** | 「天庭·地角 上下相應」을 재려면 폭/길이가 아니라 **같은 차원**의 축이 필요 |
| G-5 | `upperThirdPct`·`middleThirdPct`·`lowerThirdPct` 를 24축으로 승격 | 재고 있는데 안 쓴다 |
★G-1~G-3 은 ★새 모델·전처리가 필요합니다. 관상 커버리지를 올리는 ★유일한 길입니다.

### 6-2. P-786-B · 꿈 `DREAM_KEYS` 가동
12항목 사전이 선언만 되고 참조 0건입니다. `story` 토큰 매칭 → 상징·길흉·행운숫자를 **엔진 확정값**으로 주입.
현재 `luckyNumbers`·`fortuneType` 이 전부 LLM 임의값이라 사용자 간 겹칩니다.

### 6-3. P-786-C · 데일리 `variance` 전용 ctx 키
`api/fortune.js` 의 `daily_message` 분기에 `c.variance` 키를 열고, `js/chat.js:452` 한 줄만 옮기면 됩니다
(`_cwDailyVariance()` 는 그대로 재사용). 지금은 `personaTone` 반송이라 의미상 자리가 틀립니다.

### 6-4. P-786-D · ★게이트 침식 복구
소멸한 eval 39종 + `eval/_failclosed.js` 를 **git 이력에서 복원**하거나, 복원 불가면 **pin 표에서 명시적으로 제외하고 그 사실을 기록**하십시오. ★조용히 regen 으로 지우지 마십시오.

### 6-5. P-786-E · LLM 출력 실측
type 별 서로 다른 입력 30건 × 3회 호출 → 임베딩 코사인 유사도. ★이번에 고친 건 **프롬프트 입력의 다양성**이고, 모델이 그 씨앗을 실제 문체에 반영하는지는 **아직 측정하지 않았습니다.**

### 6-6. 검토 대상
- `CW_LLM_EFFORT='low'` — 추론을 줄이면 모델이 고빈도 사전분포로 빨리 수렴합니다. 엔진 주입 후 작명·꿈에 한해 `medium` A/B.
- `temperature`·`top_p` 미설정(기본 1.0). 명시할지 결정.
- compat 에 주체 구분을 넣고 엔진 사실을 켤지 (§4-7 보류 사유).

---

## 7. ★★★하지 말 것

```
✗ ★`_v786_diag/face_core_v786.built.js` 나 index.html 안의 코어를 ★직접 고치기
   — 정본은 `_v786_diag/face_core_v786.js` 입니다. 고친 뒤 p03 으로 재빌드하십시오.
✗ ★`CW_FACE_REF` 의 분위수를 손으로 고치기 — p03 이 생성합니다.
✗ ★관상 임계를 코드에 상수로 다시 심기 — 그것이 이번 붕괴의 원인입니다.
✗ ★사진 종횡비 보정(`aiAspect`)을 빼기 — 빼면 §2-1 이 즉시 재발합니다.
✗ ★토정 작괘를 클라·서버 **한쪽만** 고치기 — 서버가 클라값을 재유도해 교체합니다.
✗ ★`CW_ENGINE_TYPES` 에 saju 3종 밖의 type 을 추가 — 전 요청이 400 이 됩니다(v7.73 관통 #8).
✗ ★compat ctx 에 1인 기둥 3키를 싣기 — 「두 사람의 확정값」으로 읽힙니다.
✗ ★`node tools/regen_gate_pins.js` 를 무심코 돌리기 — §5-3 침식 39종이 통째로 지워집니다.
✗ ★「합성 CV 8% 결과」를 실사진 실측처럼 인용 — TIER-C 입니다(§1-2).
✗ ★「관상 라벨이 이제 정확하다」고 말하기 — 정확해진 것은 ★분포이고, ★절대 의미는 §6-1 이후입니다.
✗ ★사주 4기둥·오행·십성 다양성을 다시 재기 — 이미 양호 확인(§3 하단).
✗ ★타로 카드 **조합** 다양성을 다시 재기 — 이미 양호 확인. 문제는 위치별 빈도였고 수리 완료.
```

---

## 8. ★재현 명령

```bash
cd "D:\AI ChunWoon"

# ① 관상 현행 분류기 진단 (붕괴 실측 · 임계 대비 분포 · CV sweep · 종횡비 sweep · dead 분기)
node _v786_diag/p01_face_diversity.js --n 20000
#   기대: 4축 동일확률 60.27%(정사각) · square 97.67% · mouth small 99.76% · hooked 0건

# ② 관상 v786 재빌드 + 전/후 비교  ★코어를 고쳤으면 반드시 이걸 돌리십시오
node _v786_diag/p03_build_and_eval.js
#   기대: 4축 동일확률 26.14% → 0.63% · 조합 256/256 · 종횡비 불변성 0.18% → 100.00%

# ③ 회귀
node eval/eval_ctx_birth_keys.js     # 24/24
node eval/eval_tojeong_guard.js      # 21/21
node eval/eval_ctxguard.js           # 58/58
node eval/eval_compat_guard.js       # 46/46
node eval/eval_naming_tarot_guard.js # 57/57
node eval/eval_dream_daily_guard.js  # 48/48

# ④ 인라인 스크립트 문법 (기준선: SCRIPT#2 len=453 하나만)
node -e "const fs=require('fs');const s=fs.readFileSync('index.html','utf8');const re=/<script(?![^>]*\bsrc=)[^>]*>([\s\S]*?)<\/script>/gi;let m,i=0;while((m=re.exec(s))){i++;try{new Function(m[1])}catch(e){console.log('SCRIPT#'+i+' len='+m[1].length,e.message.slice(0,120))}}console.log('total',i)"

# ⑤ 전체 게이트 (★기준선 fail=66 — 침식 상태. 늘지 않았는지만 봅니다)
node tools/run_gate.js
```

★환경: `npm i -D jsdom` 이 **필수**입니다(없으면 ctx 게이트 2종이 「판정 불가 = FAIL」).

---

## 9. ★자산 목록

| 경로 | 내용 |
|---|---|
| ★`_v786_diag/face_core_v786.js` | ★**관상 코어 정본** (이걸 고치십시오) |
| `_v786_diag/face_core_v786.built.js` | 참조분위수 주입 빌드 산출물 (index.html 에 삽입된 것) |
| `_v786_diag/classifier_asis.js` | 현행(구) 분류기 추출본 — 전/후 비교용. **고치지 마십시오** |
| `_v786_diag/classifier_v2.js` | 초기 v2 시안 (참고용) |
| ★`_v786_diag/synth_face.js` | 합성 랜드마크 생성기 + **가정·검증사실 주석** |
| `_v786_diag/p01_face_diversity.js` | 현행 붕괴 진단 |
| `_v786_diag/p02_face_v2_eval.js` | v2 시안 평가 |
| ★`_v786_diag/p03_build_and_eval.js` | ★**빌드 + 전/후 평가** |
| `_v786_diag/face_ref_quantiles_v786.json` | p02 산출 참조분위수 (참고) |
| `_v786_diag/gate_baseline_2026-09-04.txt` | ★패치 전 게이트 전문 (fail=66) |
| `backup_pre_v786/` | index.html · fortune.js · ctxguard.js · tarot.js · chat.js 패치 전 원본 |

---

## 10. ★한 문단 요약

제이가 관찰한 「대부분 비슷하게 나온다」는 ★**사실이었고, 관상이 가장 심했습니다** — 두 사용자가 4축 라벨을 전부 똑같이 받을 확률이 ★**정사각 사진에서 60.3%**, 사진비를 섞어도 ★**26.1%** 였습니다. 원인은 세 가지였습니다. ★첫째, MediaPipe 가 x 를 사진 폭·y 를 사진 높이로 **각각** 정규화하는데 코드가 x/y 를 섞어 비율을 만들어, ★**얼굴형을 결정한 것이 얼굴이 아니라 사진 비율**이었습니다(같은 사람을 5가지 비율로 찍어 판정이 유지될 확률 ★0.18%). ★둘째, `FACEMESH_FACE_OVAL` 순회순서를 실측해 보니 코드가 「턱」이라 부른 127/356 은 ★**관자놀이**였고, 관자폭/광대폭이 항상 ≈1.0 이라 ★「각진형」이 80.7% 를 먹었습니다. ★셋째, 임계가 어떤 모집단으로도 보정된 적이 없어 ★「매부리코」는 canonical 좌표상 ★**수학적으로 도달 불가**였고, 입 임계 0.95 는 구각폭이 동공간거리보다 좁다는 사실 때문에 ★**모든 인간을 「작은 입」(98.8%)으로** 만들었으며, `mouthScore` 는 ★**고유값이 1개**였습니다. 수리는 ★등방화 · ★랜드마크 정정 · ★고정임계를 참조분위수로 교체 · ★콧대 볼록도 신설 · ★이미 재고 있던 십이궁·오관 12축 승격으로 했고, 결과는 ★4축 조합 46종 → **256/256**, 동일확률 26.1% → ★**0.63%**, 종횡비 불변성 0.18% → ★**100%**, 24축 시그니처는 ★**20,000건 전건 유일**입니다. 관상 밖에서는 ★토정 상괘가 ★**같은 해 신청자 전원 동일**이었고 팔괘 4종이 영구 미도달이었던 것을 행년·월건·일진 결합으로 고쳐 ★괘 조합 18 → **144/144**(H/Hmax 0.581 → 0.972)로 올렸고, ★궁합의 하드 clamp 로 11.5% 가 잘려나가던 것과 ★일지 관계 83.7% 가 「무관」이던 것을 각각 로지스틱 사상과 30종 확장으로 해소했으며, ★타로 셔플의 χ²/df 208.9 를 Fisher-Yates 로 1.004 로 내리고 ★화면 정/역이 LLM 값을 따르던 결함을 무료·프리미엄 양쪽에서 고쳤고, ★데일리 15종 → **17,280종**(충돌 6.44% → 0.01%), ★작명의 음수 모듈러로 한글전용 스타일 전 후보가 「총 0획·반길반흉」으로 같아지던 것을 판정 불가 명시로 바꿨습니다. ★엔진 확정값이 무료 4상품에서 0개였던 것은 ★ctx 3키 적재 + `CW_FACTS_ONLY_TYPES` 신설로 경로를 열었습니다. ★★다만 관상 참조분포는 ★**합성 유래(TIER-C)** 라 「각진형」은 지금 ★「하악각 상위 38%」라는 뜻이지 절대적 형태가 아니며, ★**LLM 출력 문자열 자체의 유사도는 아직 측정하지 않았습니다** — 고친 것은 프롬프트 입력의 다양성입니다. ★그리고 ★**게이트는 이번 작업 이전부터 침식돼 있습니다**(eval 39종 소멸 · `_failclosed.js` 부재 · 기준선 fail=66). ⟹ ★다음 세션의 최우선은 ★**P-786-A(실사진 캘리브레이션 · 생체인식정보 동의 절차 필요)** 와 ★**P-786-D(게이트 복구)** 입니다.
